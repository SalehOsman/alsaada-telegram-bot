# ⚙️ دليل قسم ضبط الوظائف

## 📌 نظرة عامة

تم إضافة قسم جديد باسم **"ضبط الوظائف"** (System Settings) إلى القائمة الرئيسية للبوت. هذا القسم مخصص لإدارة الإعدادات العامة المشتركة بين جميع أقسام النظام.

---

## 🎯 الهدف من القسم

قسم **ضبط الوظائف** يوفر مركزاً موحداً للإعدادات العامة التي تؤثر على النظام بأكمله، بدلاً من تشتيت هذه الإعدادات في أقسام مختلفة.

### الفرق بين الإعدادات الموجودة:

| القسم | النوع | الظهور | الصلاحيات | الوظيفة |
|-------|-------|--------|-----------|---------|
| **⚙️ الإعدادات** (القديم) | Keyboard Button | زر ثابت في لوحة المفاتيح | SUPER_ADMIN | إعدادات تقنية متقدمة للبنية التحتية |
| **⚙️ ضبط الوظائف** (الجديد) | Inline Button | في القائمة الرئيسية | SUPER_ADMIN | إعدادات عامة مشتركة بين الأقسام |

---

## 📋 الأقسام الفرعية المتاحة

### 1. 👥 إدارة المستخدمين
- عرض جميع المستخدمين
- البحث عن مستخدم
- إدارة الصلاحيات
- إحصائيات المستخدمين

### 2. 🔔 مركز الإشعارات
- إعدادات الإشعارات العامة
- قوالب الإشعارات
- جدولة الإشعارات
- سجل الإشعارات

### 3. 🌐 اللغة والتوطين
- اختيار اللغة
- المنطقة الزمنية
- تنسيق التاريخ
- العملة

### 4. 📊 إعدادات التقارير العامة
- قوالب التقارير
- جدولة التقارير
- إرسال التقارير
- إعدادات العرض

### 5. 💾 النسخ الاحتياطي
- إنشاء نسخة احتياطية
- عرض النسخ الاحتياطية
- استعادة البيانات
- النسخ التلقائي

### 6. ℹ️ معلومات النظام ✅ (جاهز)
- عدد المستخدمين
- عدد العاملين
- مدة تشغيل البوت
- معلومات النظام

---

## 🚀 كيفية الوصول للقسم

### للمطورين:
1. شغّل البوت: `npm run dev`
2. افتح المحادثة مع البوت في Telegram
3. اضغط على `/start` أو `📋 القائمة الرئيسية`
4. ستجد زر **⚙️ ضبط الوظائف** في القائمة الرئيسية

### ملاحظة:
- القسم **مخفي** عن المستخدمين العاديين
- **فقط SUPER_ADMIN** يمكنه رؤية والوصول لهذا القسم

---

## 🏗️ البنية التقنية

```
src/bot/features/system-settings/
├── config.ts                           # تكوين القسم والأقسام الفرعية
├── index.ts                            # النقطة الرئيسية للتصدير
├── README.md                           # توثيق تقني مفصل
└── handlers/
    ├── user-management.handler.ts      # ✅ جاهز للاستخدام
    ├── notifications-center.handler.ts # ✅ جاهز للاستخدام
    ├── language-settings.handler.ts    # ✅ جاهز للاستخدام
    ├── reports-settings.handler.ts     # ✅ جاهز للاستخدام
    ├── backup-restore.handler.ts       # ✅ جاهز للاستخدام
    └── system-info.handler.ts          # ✅ وظيفي بالكامل
```

---

## ⚙️ التكوين

### ملف `config.ts`:

```typescript
export const systemSettingsConfig: FeatureConfig = {
  id: 'system-settings',
  name: '⚙️ ضبط الوظائف',
  icon: '⚙️',
  description: 'إعدادات النظام العامة والمشتركة بين الأقسام',
  category: 'management',
  enabled: true,              // مُفعّل
  order: 90,                  // يظهر قبل الأخير
  permissions: ['SUPER_ADMIN'], // فقط للسوبر أدمن
  
  subFeatures: [
    // 6 أقسام فرعية...
  ]
}
```

---

## 🔄 التحميل التلقائي

القسم يتم تحميله **تلقائياً** عند بدء البوت من خلال:

1. **Feature Loader** يفحص مجلد `features/`
2. يجد مجلد `system-settings/`
3. يقرأ ملف `index.ts`
4. يسجل القسم في **Feature Registry**
5. يضيفه للـ **Main Menu**

---

## 📊 الحالة الحالية للتطوير

### ✅ جاهز للاستخدام:
- البنية الأساسية للقسم
- جميع الأقسام الفرعية معرّفة
- التكامل مع القائمة الرئيسية
- قسم "معلومات النظام" يعمل بالكامل

### ⚠️ قيد التطوير (Placeholders):
- وظائف إدارة المستخدمين
- وظائف مركز الإشعارات
- وظائف اللغة والتوطين
- وظائف إعدادات التقارير
- وظائف النسخ الاحتياطي

**ملاحظة:** جميع الأقسام الفرعية تعرض رسالة "⚠️ هذه الميزة قيد التطوير" عند الضغط على الأزرار.

---

## 🎨 تجربة المستخدم

### عند الضغط على "⚙️ ضبط الوظائف":
```
⚙️ ضبط الوظائف

إعدادات النظام العامة والمشتركة بين الأقسام

الأقسام الفرعية:
┌────────────────────────────────┐
│ 👥 إدارة المستخدمين            │
├────────────────────────────────┤
│ 🔔 مركز الإشعارات              │
├────────────────────────────────┤
│ 🌐 اللغة والتوطين              │
├────────────────────────────────┤
│ 📊 إعدادات التقارير العامة     │
├────────────────────────────────┤
│ 💾 النسخ الاحتياطي             │
├────────────────────────────────┤
│ ℹ️ معلومات النظام              │
└────────────────────────────────┘
         ⬅️ رجوع
```

### مثال: قسم "معلومات النظام":
```
ℹ️ معلومات النظام

📊 إحصائيات عامة:

👥 المستخدمين: 15
🧑‍💼 العاملين: 120
✅ العاملين النشطين: 115
⏱️ مدة التشغيل: 5 ساعة 23 دقيقة

📅 التاريخ: 27/10/2025
🕐 الوقت: 10:30:15

┌────────────────────────────────┐
│ 🔄 تحديث المعلومات             │
├────────────────────────────────┤
│ 📊 إحصائيات مفصلة              │
└────────────────────────────────┘
         ⬅️ رجوع
```

---

## 🔮 الخطوات التالية

### للمطور:
1. **تطوير الوظائف الأساسية:**
   - إدارة المستخدمين (عرض - بحث - تعديل الصلاحيات)
   - مركز الإشعارات (قوالب - جدولة)
   - النسخ الاحتياطي (إنشاء - استعادة)

2. **إضافة إعدادات خاصة بكل قسم:**
   ```
   👥 شئون العاملين
   ├── ... (الوظائف الحالية)
   └── ⚙️ ضبط القسم
       ├── تفعيل/إيقاف الحقول
       ├── تخصيص التقارير
       └── إعدادات الإشعارات
   ```

3. **تحسينات الأمان:**
   - تأكيد قبل الحذف/الاستعادة
   - سجل تغييرات الصلاحيات
   - النسخ الاحتياطي التلقائي

---

## 💡 نصائح مهمة

### للمطورين:
1. **جميع الـ Handlers** تتبع نفس النمط
2. **استخدم Callback Query** بالصيغة: `menu:sub:system-settings:{id}`
3. **Placeholder handlers** موجودة للتطوير المستقبلي
4. **لا تنسَ تسجيل Handler الجديد** في `index.ts`

### للمستخدمين (SUPER_ADMIN):
1. القسم **آمن** - كل الوظائف محمية بصلاحيات
2. **النسخ الاحتياطي** سيكون مهم جداً قبل أي تعديلات كبيرة
3. **معلومات النظام** مفيدة لمتابعة حالة البوت

---

## 📞 الدعم

إذا واجهت أي مشكلة أو لديك اقتراحات:
1. راجع `src/bot/features/system-settings/README.md` للتفاصيل التقنية
2. تحقق من logs: `logs/` directory
3. استخدم `/start` لإعادة تحميل القائمة

---

**تم الإنشاء:** 27 أكتوبر 2025  
**الحالة:** ✅ جاهز للاستخدام (بعض الوظائف قيد التطوير)  
**الإصدار:** 1.0.0
