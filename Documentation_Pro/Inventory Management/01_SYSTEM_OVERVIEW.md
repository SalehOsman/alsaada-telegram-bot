# **🎯 نظرة عامة \- نظام المخازن (Inventory System Overview)**

الإصدار: 1.0  
التاريخ: 8 نوفمبر 2025

## **1\. فلسفة التصميم**

يهدف هذا النظام إلى الإجابة على ثلاثة أسئلة حيوية للشركة:

1. **ماذا نملك؟** (تتبع الرصيد الفعلي والباركود).  
2. **أين ذهب؟** (تتبع حركات الصرف وربطها بالمعدات والموظفين والمشاريع).  
3. **كم كلفنا؟** (تتبع تكلفة الشراء ومتوسط تكلفة الاستهلاك).

### **التمييز الهام: المخزون مقابل السلف المالية**

يجب التمييز بوضوح بين هذا النظام ونظام HR\_Transaction الحالي \[cite: Documentation\_Pro/HR-Management/13\_SETTLEMENTS\_AND\_TRANSACTIONS.md\]:

* **نظام HR\_Transaction (السلف):**  
  * **الغرض:** معاملات مالية تُخصم من الراتب (سلفة نقدية، سجائر).  
  * **الهدف:** الخصم من الموظف.  
* **نظام Inventory (المخازن):**  
  * **الغرض:** إدارة أصول مادية (قطع غيار، وقود، أدوات).  
  * **الهدف:** تتبع الاستهلاك والتكلفة.

الجسر الوحيد (The Bridge):  
يحدث الربط فقط عندما تتحول "العهدة" (من المخزن) إلى "دين" (على الموظف).  
مثال: إذا أضاع العامل صندوق أدوات (عهدة)، يتم إنشاء سجل "دين" في HR\_Transaction \[cite: Documentation\_Pro/HR-Management/11\_PAYROLL\_SYSTEM.md\] بآخر متوسط تكلفة معروف للصندوق.

## **2\. الميزات الوظيفية (Functional Requirements)**

### **2.1. إدارة المخازن والأصناف**

* إمكانية تعريف عدد لا نهائي من **المخازن** (Warehouse) وتصنيفها (قطع غيار، وقود، زيوت، أدوات).  
* إمكانية تعريف **فئات للأصناف** (ItemCategory) لسهولة التنظيم.  
* كتالوج موحد **للأصناف** (Item) يشمل:  
  * اسم الصنف ووحدة القياس (قطعة، لتر).  
  * **رقم الصنف/الباركود (SKU)** كنص فريد لتمكين البحث السريع بالماسح الضوئي.  
  * حد إعادة الطلب (lowStockThreshold) لإصدار تنبيهات آلية.

### **2.2. إدارة الموردين والمشتريات**

* إدارة سجل كامل **للموردين** (Supplier) وبياناتهم (هاتف، رقم ضريبي).  
* تسجيل **أوامر الشراء** (PurchaseOrder) لربط الفواتير بالموردين.

### **2.3. إدارة الأرصدة والتكاليف**

* يجب أن يتتبع النظام **الرصيد الفعلي** (Stock.quantity) لكل صنف في كل مخزن.  
* يجب أن يقوم النظام بحساب **متوسط التكلفة المرجح (WAC)** (Stock.averageCost) تلقائياً عند كل عملية شراء (PURCHASE\_IN).

### **2.4. إدارة الحركات (الصرف والإضافة)**

* تسجيل جميع حركات المخزون (StockTransaction) بأنواعها:  
  * PURCHASE\_IN: إضافة مشتريات (ترفع الرصيد وتحدث التكلفة).  
  * CONSUMPTION\_OUT: صرف للاستهلاك (تخفض الرصيد).  
  * CUSTODY\_OUT: صرف كعهدة (تخفض الرصيد).  
  * RETURN\_IN: إرجاع للمخزن (ترفع الرصيد).  
* يجب أن يتم تسجيل **تكلفة الصنف** (unitPrice) في كل حركة صرف، بناءً على averageCost في تلك اللحظة.

### **2.5. تتبع وارتباط الحركات**

* يجب ربط *كل* حركة صرف (CONSUMPTION\_OUT أو CUSTODY\_OUT) ببيان واضح (TransactionLink) يوضح:  
  * **الموظف المستلم** (من جدول Employee \[cite: Documentation\_Pro/HR-Management/03\_DATABASE\_SCHEMA.md\]).  
  * **المعدة المستهلكة** (إن وجدت) (من جدول Equipment المستقبلي).  
  * **المشروع الموجه له التكلفة** (من جدول Project \[cite: prisma/schema.prisma\]).

### **2.6. إدارة العهد**

* تتبع **العهد** المسلمة للموظفين (EmployeeCustody) بشكل منفصل.  
* يجب أن يكون للعهدة تاريخ استلام وتاريخ إرجاع.  
* يجب أن تكون العهدة مرتبطة بالموظف في نظام شئون العاملين (Employee \[cite: Documentation\_Pro/HR-Management/03\_DATABASE\_SCHEMA.md\]).

## **3\. نقاط التكامل (Integration Points)**

1. **شئون العاملين (HR):**  
   * **Employee:** يُستخدم لربط EmployeeCustody (العهد) و TransactionLink (المستلم).  
   * **HR\_Transaction:** يُستخدم *فقط* لإنشاء "دين" عند فقدان العهدة، ليتم خصمه من الراتب \[cite: Documentation\_Pro/HR-Management/11\_PAYROLL\_SYSTEM.md\].  
2. **المعدات والصيانة (Equipment):**  
   * Equipment: جدول مستقبلي يُستخدم لربط TransactionLink بالمعدة المستهلكة لقطع الغيار والوقود.  
3. **المشاريع (Projects):**  
   * Project: جدول موجود \[cite: prisma/schema.prisma\] يُستخدم لربط TransactionLink بالمشروع لتوجيه التكاليف.  
4. **الباركود (Barcode Scanner):**  
   * وحدة الباركود ستستخدم حقل Item.sku للبحث السريع عن الأصناف.  
5. **الحسابات (Accounting):**  
   * سيقوم هذا القسم بتزويد قسم الحسابات بـ:  
     * قيمة المشتريات (من PurchaseOrder و StockTransaction).  
     * قيمة المخزون الحالي (من Stock.quantity \* Stock.averageCost).  
     * قيمة الاستهلاك (من StockTransaction.unitPrice عند الصرف).