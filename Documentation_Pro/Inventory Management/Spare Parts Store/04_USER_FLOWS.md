# 🔄 مخزن قطع الغيار - تدفقات المستخدم

**النسخة:** 2.0  
**آخر تحديث:** نوفمبر 2025

---

## 1️⃣ إضافة قطعة جديدة

### الطريقة الأولى: مسح الباركود

```
المستخدم → إدارة قطع الغيار → إضافة قطعة جديدة → مسح الباركود
   ↓
يرسل صورة الباركود
   ↓
النظام يقرأ الباركود تلقائياً
   ↓
التحقق من عدم وجود تكرار
   ↓
[إذا موجود] → عرض تحذير + خيارات (عرض القطعة / إضافة كمية / إلغاء)
[إذا غير موجود] → متابعة للخطوة التالية
   ↓
إدخال الاسم العربي (نص)
   ↓
اختيار التصنيف (أزرار)
   ↓
اختيار الموقع (أزرار)
   ↓
اختيار الحالة (جديدة/مستعملة/استيراد)
   ↓
إدخال الكمية (رقم)
   ↓
إدخال السعر (رقم أو تخطي)
   ↓
إدخال ملاحظات (نص أو تخطي)
   ↓
إرسال صور (صور أو تخطي)
   ↓
مراجعة البيانات النهائية
   ↓
تأكيد الحفظ
   ↓
✅ تم الحفظ + إرسال تقرير للمسؤولين
```

### الطريقة الثانية: إدخال يدوي

```
المستخدم → إدخال يدوي
   ↓
اختيار طريقة الباركود:
  - إدخال يدوي
  - توليد تلقائي
   ↓
[بقية الخطوات مماثلة للطريقة الأولى]
```

---

## 2️⃣ البحث عن قطعة

### البحث بالباركود

```
المستخدم → البحث → بالباركود
   ↓
مسح صورة أو إدخال يدوي
   ↓
النظام يبحث في قاعدة البيانات
   ↓
[إذا موجود] → عرض تفاصيل كاملة + أزرار (تعديل/حذف)
[إذا غير موجود] → رسالة "لم يتم العثور على نتائج"
```

### البحث بالكود

```
المستخدم → البحث → بالكود
   ↓
إدخال الكود (مثل: CAR-00001)
   ↓
[نفس النتيجة]
```

### البحث بالاسم

```
المستخدم → البحث → بالاسم
   ↓
إدخال جزء من الاسم (عربي أو إنجليزي)
   ↓
عرض جميع النتائج المطابقة
```

---

## 3️⃣ عرض جميع القطع

```
المستخدم → عرض جميع القطع
   ↓
عرض قائمة بجميع القطع (pagination)
   ↓
كل قطعة: الاسم + الكود + الكمية + زر "عرض التفاصيل"
   ↓
النقر على قطعة → عرض التفاصيل الكاملة
```

---

## 4️⃣ تعديل قطعة

```
المستخدم → عرض قطعة → تعديل
   ↓
عرض قائمة بالحقول القابلة للتعديل:
  - الاسم
  - التصنيف
  - الموقع
  - الحالة
  - الكمية
  - السعر
  - الملاحظات
   ↓
اختيار الحقل → تعديل القيمة
   ↓
حفظ التعديلات
   ↓
✅ تم التحديث + إشعار للمسؤولين
```

---

## 5️⃣ حذف قطعة

```
المستخدم → عرض قطعة → حذف
   ↓
رسالة تأكيد
   ↓
تأكيد الحذف
   ↓
حذف ناعم (Soft Delete)
   ↓
✅ تم الحذف + إشعار للمسؤولين
```

---

## 📊 ملاحظات مهمة

### Session Management
- كل تدفق يحفظ حالته في `ctx.session.inventoryForm`
- يتم تنظيف الـ session بعد إتمام العملية أو الإلغاء

### الخطوات الصالحة
```typescript
const validSteps = [
  'awaiting_barcode_image',
  'awaiting_barcode_text',
  'awaiting_name_ar',
  'awaiting_name_en',
  'awaiting_category',
  'awaiting_location',
  'awaiting_condition',
  'awaiting_quantity',
  'awaiting_unit',
  'awaiting_price',
  'awaiting_notes',
  'awaiting_images',
  'search_by_code',
  'search_by_name',
]
```

### التحقق من الصلاحيات
- يتم التحقق من الصلاحية قبل كل عملية
- رسالة خطأ واضحة في حال عدم وجود صلاحية

---

## 🎯 أفضل الممارسات للمستخدم

1. **استخدم مسح الباركود** بدلاً من الإدخال اليدوي
2. **راجع البيانات** قبل الحفظ النهائي
3. **أضف صوراً واضحة** للقطع
4. **استخدم البحث بالباركود** للوصول السريع
5. **تحقق من التنبيهات** بانتظام
