# 🔄 نظام إدارة دورة العمل والإجازات

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الهيكل العام](#الهيكل-العام)
3. [قاعدة البيانات](#قاعدة-البيانات)
4. [الوظائف الرئيسية](#الوظائف-الرئيسية)
5. [إدارة دورات الوظائف](#إدارة-دورات-الوظائف)
6. [إدارة دورات الموظفين](#إدارة-دورات-الموظفين)
7. [نظام التدقيق والسجلات](#نظام-التدقيق-والسجلات)
8. [التقارير والتصدير](#التقارير-والتصدير)
9. [الأمثلة العملية](#الأمثلة-العملية)
10. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🎯 نظرة عامة

### **ما هو نظام دورة العمل والإجازات؟**

نظام **إدارة دورة العمل والإجازات** هو نظام شامل ومتطور لإدارة أيام العمل والإجازات للموظفين بطريقة مرنة ودقيقة. يسمح النظام بتحديد:

- ✅ **أيام العمل الافتراضية** لكل وظيفة
- ✅ **أيام الإجازة الافتراضية** لكل وظيفة
- ✅ **دورات مخصصة** لموظفين معينين تختلف عن وظيفتهم
- ✅ **تتبع كامل** لجميع التغييرات (Audit Log)
- ✅ **تقارير شاملة** قابلة للتصدير إلى Excel

### **الميزات الأساسية:**

| الميزة | الوصف |
|--------|--------|
| 🏢 **دورات الوظائف** | تحديد قيم افتراضية لكل وظيفة تُطبق على الموظفين الجدد |
| 👤 **دورات مخصصة** | إمكانية تخصيص دورة لموظف معين تختلف عن وظيفته |
| 🔍 **فلاتر متقدمة** | عرض الموظفين حسب (الكل / افتراضي / مخصص / غير محدد) |
| 📊 **تقارير Excel** | تصدير شامل لجميع البيانات مع تفاصيل الوظائف والأقسام |
| 📝 **سجل التدقيق** | حفظ كل تغيير مع: القيمة القديمة، الجديدة، التاريخ، المستخدم |
| 🔄 **استعادة الافتراضي** | إمكانية إعادة الموظف لقيم وظيفته الافتراضية بنقرة واحدة |

---

## 🏗️ الهيكل العام

### **مكونات النظام:**

```
work-leave-cycle/
├── index.ts                    # النقطة الرئيسية والتوجيه
├── positions.handler.ts        # إدارة دورات الوظائف
├── employees.handler.ts        # قوائم وعرض الموظفين
├── employee-edit.handler.ts    # تعديل دورة موظف معين
├── search.handler.ts           # البحث بالاسم
└── export.handler.ts           # تصدير Excel
```

### **المسار في النظام:**

```
القائمة الرئيسية
  └── شئون العاملين
      └── إدارة قسم شئون العاملين (الإعدادات)
          └── 🔄 إدارة دورة العمل والإجازات
              ├── 🏢 إدارة دورات الوظائف
              ├── 👤 إدارة دورات الموظفين
              └── 📊 تصدير تقرير Excel
```

---

## 🗄️ قاعدة البيانات

### **1. التعديلات على جدول Employee**

```prisma
model Employee {
  id                  Int      @id @default(autoincrement())
  fullName            String
  
  // دورة العمل والإجازات
  workDaysPerCycle    Int?     // عدد أيام العمل في الدورة (مثل: 20، 30)
  leaveDaysPerCycle   Int?     // عدد أيام الإجازة في الدورة (مثل: 7، 10)
  hasCustomCycle      Boolean  @default(false) // علامة: هل الموظف لديه دورة مخصصة؟
  
  // علاقات
  position            Position @relation(...)
  cycleChangeLogs     HR_CycleChangeLog[] // سجل التغييرات
  
  @@map("HR_Employee")
}
```

### **2. التعديلات على جدول Position**

```prisma
model Position {
  id                        Int      @id @default(autoincrement())
  titleAr                   String
  
  // القيم الافتراضية للدورة
  defaultWorkDaysPerCycle   Int?     // أيام العمل الافتراضية للوظيفة
  defaultLeaveDaysPerCycle  Int?     // أيام الإجازة الافتراضية للوظيفة
  
  // علاقات
  employees                 Employee[]
  
  @@map("HR_Position")
}
```

### **3. جدول سجل التغييرات (Audit Log)**

```prisma
model HR_CycleChangeLog {
  id            Int      @id @default(autoincrement())
  
  // نوع الكيان (Position أو Employee)
  entityType    String   // "Position" | "Employee"
  entityId      Int      // ID الوظيفة أو الموظف
  
  // القيم القديمة
  oldWorkDays   Int?
  oldLeaveDays  Int?
  
  // القيم الجديدة
  newWorkDays   Int?
  newLeaveDays  Int?
  
  // معلومات التدقيق
  changedBy     BigInt   // Telegram ID للمستخدم الذي أجرى التغيير
  changedAt     DateTime @default(now())
  reason        String?  // سبب التغيير (اختياري)
  
  @@map("HR_CycleChangeLog")
}
```

### **الفوائد من هذا التصميم:**

| الفائدة | الشرح |
|---------|--------|
| 🎯 **الوضوح** | فصل واضح بين القيم الافتراضية والقيم المخصصة |
| 🔒 **الحماية** | علامة `hasCustomCycle` تمنع الكتابة الخاطئة على قيم مخصصة |
| 📊 **التتبع الكامل** | كل تغيير مسجل مع التفاصيل الكاملة |
| ⚡ **الأداء** | استعلامات سريعة باستخدام indexes على الحقول المهمة |

---

## ⚙️ الوظائف الرئيسية

### **القائمة الرئيسية**

```typescript
// Handler: hr:work_leave_cycle:main
const mainMenu = {
  inline_keyboard: [
    [{ text: '🏢 إدارة دورات الوظائف', callback_data: 'hr:cycle:positions:list' }],
    [{ text: '👤 إدارة دورات الموظفين', callback_data: 'hr:cycle:employees:menu' }],
    [{ text: '📊 تصدير تقرير Excel', callback_data: 'hr:cycle:export' }],
    [{ text: '🔙 رجوع', callback_data: 'hr:section:manage' }],
  ],
}
```

### **الصلاحيات:**

- ✅ **SUPER_ADMIN فقط**
- ❌ لا يمكن تعيين أدمن آخرين على هذه الوظيفة

---

## 🏢 إدارة دورات الوظائف

### **1. قائمة الوظائف**

```typescript
// Handler: hr:cycle:positions:list
```

**العرض:**
```
🏢 قائمة الوظائف

اختر وظيفة للتفاصيل:

[طباخ (30/10) [2]]  [رئيس طهاة (30/10) [2]]
[مساعد صيانة (20/10) [2]]  [فني صيانة (؟/؟) [2]]
...
```

**التفاصيل:**
- 📊 **الصيغة:** `اسم الوظيفة (أيام العمل/أيام الإجازة) [عدد الموظفين]`
- ❓ **الرمز (؟/؟):** يعني أن الوظيفة لم يتم تحديد قيم افتراضية لها بعد
- 👥 **[العدد]:** عدد الموظفين النشطين في هذه الوظيفة

### **2. عرض تفاصيل الوظيفة**

```typescript
// Handler: hr:cycle:position:view:(\d+)
```

**العرض:**
```
🏢 مساعد صيانة عامة

أيام العمل: 20
أيام الإجازة: 10

👥 الموظفون: 2 (مخصص: 0)

[✏️ تعديل]
[🔙 رجوع]
```

**المعلومات المعروضة:**
- 📌 **اسم الوظيفة**
- 📅 **أيام العمل الافتراضية**
- 🏖️ **أيام الإجازة الافتراضية**
- 👥 **إجمالي الموظفين في الوظيفة**
- 🔧 **عدد الموظفين الذين لديهم دورة مخصصة**

### **3. تعديل دورة الوظيفة**

```typescript
// Handler: hr:cycle:position:edit:(\d+)
```

**خطوات التعديل:**

1️⃣ **اختيار الحقل:**
```
اختر الحقل:
[📅 أيام العمل]
[🏖️ أيام الإجازة]
[🔙 رجوع]
```

2️⃣ **إدخال القيمة:**
```
أرسل القيمة (رقم صحيح موجب):
[❌ إلغاء]
```

3️⃣ **التأكيد:**
```
✅ تم التحديث
مساعد صيانة عامة
[🔙 رجوع]
```

### **⚠️ ملاحظات مهمة:**

| النقطة | التفاصيل |
|--------|----------|
| 🆕 **الموظفون الجدد** | التغيير على الوظيفة يؤثر فقط على الموظفين الجدد الذين سيُضافون |
| 👥 **الموظفون الحاليون** | لا يتأثرون بالتغيير - يحتفظون بقيمهم الحالية |
| 🔧 **الموظفون المخصصون** | محميون بعلامة `hasCustomCycle` ولن يتأثروا أبدًا |
| 📝 **السجل** | كل تغيير يُسجل في `HR_CycleChangeLog` |

---

## 👤 إدارة دورات الموظفين

### **1. قائمة خيارات العرض**

```typescript
// Handler: hr:cycle:employees:menu
```

**القائمة:**
```
👤 إدارة دورات الموظفين

اختر طريقة العرض:

[📋 جميع الموظفين]
[✅ افتراضي فقط]
[🔧 مخصص فقط]
[❌ غير محدد]
[🔍 بحث بالاسم]
[🔙 رجوع]
```

### **2. الفلاتر المتاحة**

| الفلتر | Callback | الشرط | الوصف |
|--------|----------|-------|-------|
| **الكل** | `hr:cycle:employees:all` | `isActive: true` | جميع الموظفين النشطين |
| **افتراضي** | `hr:cycle:employees:default` | `hasCustomCycle: false` | فقط من يستخدمون دورة الوظيفة |
| **مخصص** | `hr:cycle:employees:custom` | `hasCustomCycle: true` | فقط من لديهم دورة مخصصة |
| **غير محدد** | `hr:cycle:employees:none` | `workDays: null OR leaveDays: null` | من لم تُحدد دورتهم بعد |

### **3. عرض قائمة الموظفين**

```typescript
// Handler: hr:cycle:employees:(all|default|custom|none)(?::(\d+))?
```

**العرض:**
```
👥 الموظفين (all)

الصفحة 1 من 3

[صالح رجب - فني صيانة لودر (20/10) 🔧]
[أحمد محمد - طباخ (30/10)]
[علي حسن - مساعد صيانة (20/10)]
...

[⬅️]  [➡️]
[🔙 رجوع]
```

**التفاصيل:**
- 📊 **الصيغة:** `الاسم - الوظيفة (أيام العمل/أيام الإجازة) 🔧`
- 🔧 **الرمز 🔧:** يظهر فقط للموظفين ذوي الدورة المخصصة
- 📄 **Pagination:** 8 موظفين لكل صفحة
- ⬅️➡️ **التنقل:** أزرار التنقل بين الصفحات

### **4. عرض تفاصيل الموظف**

```typescript
// Handler: hr:cycle:employee:view:(\d+)
```

**العرض:**
```
👤 صالح رجب محمد عثمان

الوظيفة: فني صيانة لودر
أيام العمل: 20
أيام الإجازة: 10
مخصص: نعم 🔧

[✏️ تعديل]
[🔄 استعادة الافتراضي]
[🔙 رجوع]
```

### **5. تعديل دورة الموظف**

```typescript
// Handler: hr:cycle:employee:edit:(\d+)
```

**الخطوات:**

1️⃣ **اختيار الحقل:**
```
اختر الحقل:
[📅 أيام العمل]
[🏖️ أيام الإجازة]
[🔙 رجوع]
```

2️⃣ **إدخال القيمة:**
```
أرسل القيمة (رقم صحيح موجب):
[❌ إلغاء]
```

3️⃣ **التأكيد:**
```
✅ تم التحديث
صالح رجب محمد عثمان
[🔙 رجوع]
```

**⚙️ ما يحدث تلقائياً:**
```typescript
await Database.prisma.employee.update({
  where: { id: employeeId },
  data: {
    workDaysPerCycle: value,  // أو leaveDaysPerCycle
    hasCustomCycle: true,     // ✅ يتم تفعيل العلامة تلقائياً
  },
})
```

### **6. استعادة القيم الافتراضية**

```typescript
// Handler: hr:cycle:employee:reset:(\d+)
```

**السيناريو 1: الوظيفة لديها قيم افتراضية**

```
✅ تمت استعادة الإعدادات الافتراضية

أيام العمل: 20
أيام الإجازة: 10
[🔙 رجوع]
```

**السيناريو 2: الوظيفة ليس لديها قيم افتراضية**

```
⚠️ الوظيفة لا تحتوي على قيم افتراضية

الوظيفة: فني صيانة لودر

يجب أولاً تحديد القيم الافتراضية للوظيفة 
من قائمة "إدارة دورات الوظائف"

[🔙 رجوع]
```

**⚙️ ما يحدث عند الاستعادة:**
```typescript
await Database.prisma.employee.update({
  where: { id: employeeId },
  data: {
    workDaysPerCycle: position.defaultWorkDaysPerCycle,
    leaveDaysPerCycle: position.defaultLeaveDaysPerCycle,
    hasCustomCycle: false,  // ✅ إلغاء العلامة
  },
})
```

### **7. البحث بالاسم**

```typescript
// Handler: hr:cycle:search
```

**الخطوات:**

1️⃣ **طلب الإدخال:**
```
أرسل اسم الموظف للبحث:
[❌ إلغاء]
```

2️⃣ **النتائج:**
```
✅ نتائج البحث (3):

[صالح رجب - فني صيانة (20/10) 🔧]
[صالح أحمد - طباخ (30/10)]
[صالح علي - مساعد صيانة (؟/؟)]

[🔙 رجوع]
```

**المميزات:**
- 🔍 **البحث الجزئي:** يبحث في أي جزء من الاسم
- 📊 **حد أقصى:** 15 نتيجة
- ⚡ **سريع:** ترتيب أبجدي تلقائي

---

## 📝 نظام التدقيق والسجلات

### **سجل التغييرات (Audit Log)**

كل تغيير يتم حفظه تلقائياً في جدول `HR_CycleChangeLog`:

```typescript
await Database.prisma.hR_CycleChangeLog.create({
  data: {
    entityType: 'Employee',         // أو 'Position'
    entityId: employeeId,           // ID الكيان
    oldWorkDays: 20,                // القيمة القديمة
    oldLeaveDays: 10,
    newWorkDays: 25,                // القيمة الجديدة
    newLeaveDays: 12,
    changedBy: BigInt(ctx.from!.id), // Telegram ID
    changedAt: new Date(),          // تلقائي
    reason: 'تعديل مخصص',          // اختياري
  },
})
```

### **أمثلة السجلات:**

#### **مثال 1: تعديل وظيفة**

| الحقل | القيمة |
|------|--------|
| entityType | Position |
| entityId | 3 |
| oldWorkDays | 20 |
| oldLeaveDays | null |
| newWorkDays | 20 |
| newLeaveDays | 10 |
| changedBy | 7594239391 |
| reason | null |

#### **مثال 2: تعديل موظف**

| الحقل | القيمة |
|------|--------|
| entityType | Employee |
| entityId | 15 |
| oldWorkDays | 20 |
| oldLeaveDays | 10 |
| newWorkDays | 25 |
| newLeaveDays | 12 |
| changedBy | 7594239391 |
| reason | null |

#### **مثال 3: استعادة افتراضي**

| الحقل | القيمة |
|------|--------|
| entityType | Employee |
| entityId | 15 |
| oldWorkDays | 25 |
| oldLeaveDays | 12 |
| newWorkDays | 20 |
| newLeaveDays | 10 |
| changedBy | 7594239391 |
| reason | Reset to position defaults |

### **الفوائد:**

- ✅ **المساءلة:** معرفة من أجرى كل تغيير
- ✅ **التتبع:** متابعة تطور القيم عبر الزمن
- ✅ **الاسترجاع:** إمكانية الرجوع للقيم القديمة
- ✅ **التحليل:** إنشاء تقارير عن التغييرات

---

## 📊 التقارير والتصدير

### **تصدير Excel**

```typescript
// Handler: hr:cycle:export
```

### **محتوى التقرير:**

| العمود | الوصف | مثال |
|--------|-------|------|
| **الرقم** | رقم تسلسلي | 1, 2, 3... |
| **الاسم** | الاسم الكامل للموظف | صالح رجب محمد عثمان |
| **الوظيفة** | اسم الوظيفة | فني صيانة لودر |
| **القسم** | اسم القسم | الصيانة |
| **أيام العمل** | أيام العمل للموظف | 20 |
| **أيام الإجازة** | أيام الإجازة للموظف | 10 |
| **مخصص** | هل الدورة مخصصة؟ | نعم / لا |
| **افتراضي العمل** | أيام العمل الافتراضية للوظيفة | 20 |
| **افتراضي الإجازة** | أيام الإجازة الافتراضية للوظيفة | 10 |

### **مثال على التقرير:**

| الرقم | الاسم | الوظيفة | القسم | أيام العمل | أيام الإجازة | مخصص | افتراضي العمل | افتراضي الإجازة |
|------|------|---------|-------|-------------|---------------|------|---------------|-----------------|
| 1 | صالح رجب | فني صيانة | الصيانة | 25 | 12 | نعم | 20 | 10 |
| 2 | أحمد محمد | طباخ | الإعاشة | 30 | 10 | لا | 30 | 10 |
| 3 | علي حسن | مساعد صيانة | الصيانة | غير محدد | غير محدد | لا | 20 | 10 |

### **المميزات:**

- 📊 **شامل:** جميع الموظفين النشطين
- 🎨 **منسق:** عنوان بالخط العريض (Bold)
- 📁 **منظم:** ترتيب حسب القسم ثم الاسم
- ⚡ **سريع:** استخدام ExcelJS لتوليد الملف
- 📱 **سهل التنزيل:** يُرسل مباشرة عبر Telegram

### **الكود الفني:**

```typescript
const workbook = new ExcelJS.Workbook()
const sheet = workbook.addWorksheet('دورة العمل والإجازات')

sheet.columns = [
  { header: 'الرقم', key: 'num', width: 10 },
  { header: 'الاسم', key: 'name', width: 25 },
  { header: 'الوظيفة', key: 'position', width: 20 },
  { header: 'القسم', key: 'department', width: 20 },
  { header: 'أيام العمل', key: 'workDays', width: 15 },
  { header: 'أيام الإجازة', key: 'leaveDays', width: 15 },
  { header: 'مخصص', key: 'custom', width: 12 },
  { header: 'افتراضي العمل', key: 'defaultWork', width: 15 },
  { header: 'افتراضي الإجازة', key: 'defaultLeave', width: 15 },
]

sheet.getRow(1).font = { bold: true }

const buffer = await workbook.xlsx.writeBuffer()
await ctx.replyWithDocument(
  new InputFile(new Uint8Array(buffer), `work-leave-cycles-${Date.now()}.xlsx`),
  { caption: `✅ تم التصدير\nعدد الموظفين: ${employees.length}` }
)
```

---

## 💡 الأمثلة العملية

### **مثال 1: إعداد دورة لوظيفة جديدة**

**السيناريو:** وظيفة "مهندس مساحة" جديدة تحتاج إلى تحديد دورة عمل.

**الخطوات:**
1. الذهاب إلى: `إدارة دورة العمل والإجازات` → `إدارة دورات الوظائف`
2. اختيار: `مهندس مساحة (؟/؟) [1]`
3. الضغط على: `✏️ تعديل`
4. تحديد: `📅 أيام العمل` → إدخال `21`
5. تحديد: `🏖️ أيام الإجازة` → إدخال `7`

**النتيجة:**
- ✅ جميع المهندسين الجدد سيرثون (21/7) تلقائياً
- ✅ المهندس الموجود حالياً لن يتأثر (يبقى بقيمه الحالية)

---

### **مثال 2: تخصيص دورة لموظف محدد**

**السيناريو:** موظف "صالح رجب" يحتاج دورة مختلفة عن وظيفته.

**الخطوات:**
1. الذهاب إلى: `إدارة دورات الموظفين` → `جميع الموظفين`
2. البحث عن: `صالح رجب - فني صيانة (20/10)`
3. الضغط على: `✏️ تعديل`
4. تحديد: `📅 أيام العمل` → إدخال `25`
5. تحديد: `🏖️ أيام الإجازة` → إدخال `12`

**النتيجة:**
- ✅ صالح رجب أصبح: `(25/12) 🔧`
- ✅ تم تفعيل `hasCustomCycle = true` تلقائياً
- ✅ حُفظ السجل في `HR_CycleChangeLog`

---

### **مثال 3: إعادة موظف للإعدادات الافتراضية**

**السيناريو:** موظف لديه دورة مخصصة نريد إلغاءها.

**الخطوات:**
1. فتح تفاصيل الموظف
2. الضغط على: `🔄 استعادة الافتراضي`
3. التأكيد

**النتيجة:**
- ✅ عاد الموظف لقيم وظيفته (20/10)
- ✅ تم إلغاء `hasCustomCycle = false`
- ✅ اختفى الرمز 🔧 من اسمه
- ✅ حُفظ السجل مع `reason: "Reset to position defaults"`

---

### **مثال 4: تصدير تقرير شامل**

**السيناريو:** الإدارة تريد مراجعة جميع دورات الموظفين.

**الخطوات:**
1. الذهاب إلى: `إدارة دورة العمل والإجازات`
2. الضغط على: `📊 تصدير تقرير Excel`
3. انتظار التوليد
4. تنزيل الملف `work-leave-cycles-{timestamp}.xlsx`

**النتيجة:**
- ✅ ملف Excel شامل بجميع الموظفين النشطين
- ✅ يحتوي على 9 أعمدة بكل التفاصيل
- ✅ جاهز للطباعة أو المشاركة

---

### **مثال 5: فلترة الموظفين المخصصين**

**السيناريو:** نريد مراجعة فقط الموظفين ذوي الدورات المخصصة.

**الخطوات:**
1. الذهاب إلى: `إدارة دورات الموظفين`
2. اختيار: `🔧 مخصص فقط`
3. مراجعة القائمة

**النتيجة:**
- ✅ تظهر فقط الموظفين الذين لديهم `hasCustomCycle = true`
- ✅ سهل تحديد من لديهم إعدادات خاصة
- ✅ يساعد في المراجعة الدورية

---

## ❓ الأسئلة الشائعة

### **س1: ماذا يحدث عندما أضيف موظف جديد؟**

**ج:** 
- إذا كانت الوظيفة لديها قيم افتراضية (`defaultWorkDaysPerCycle`, `defaultLeaveDaysPerCycle`)، سيرثها الموظف الجديد تلقائياً
- إذا لم تكن هناك قيم افتراضية، سيبقى الموظف بقيم `null`
- `hasCustomCycle` سيكون `false` افتراضياً

---

### **س2: هل تعديل دورة الوظيفة يؤثر على الموظفين الحاليين؟**

**ج:** 
- ❌ **لا** - الموظفون الحاليون يحتفظون بقيمهم الحالية
- ✅ **نعم** - فقط الموظفون الجدد الذين سيُضافون في المستقبل

---

### **س3: ماذا يعني الرمز 🔧 بجوار اسم الموظف؟**

**ج:** 
- يعني أن هذا الموظف لديه **دورة مخصصة** تختلف عن الوظيفة
- `hasCustomCycle = true`
- محمي من التعديلات التلقائية

---

### **س4: ماذا أفعل إذا لم يكن للوظيفة قيم افتراضية؟**

**ج:** 
1. اذهب إلى: `إدارة دورات الوظائف`
2. اختر الوظيفة
3. حدد القيم الافتراضية
4. الآن يمكن استخدام "استعادة الافتراضي" للموظفين

---

### **س5: هل يمكن حذف سجلات التدقيق؟**

**ج:** 
- ⚠️ **لا يُنصح به** - السجلات مهمة للمساءلة والتتبع
- إذا كان ضرورياً، يجب حذفها يدوياً من قاعدة البيانات

---

### **س6: كيف أعرف من عدّل دورة موظف معين؟**

**ج:** 
```sql
SELECT * FROM HR_CycleChangeLog 
WHERE entityType = 'Employee' 
  AND entityId = 15 
ORDER BY changedAt DESC;
```

---

### **س7: هل يمكن تصدير سجل التغييرات؟**

**ج:** 
- حالياً، التصدير يشمل فقط البيانات الحالية
- يمكن إضافة ميزة تصدير السجل في المستقبل

---

### **س8: ماذا يحدث عند حذف موظف؟**

**ج:** 
- سجلات التدقيق تبقى محفوظة (لا تُحذف)
- القيم في جدول `Employee` تُحذف أو تُعلّم كـ `isActive: false`

---

### **س9: هل يمكن ربط هذا النظام بنظام الإجازات؟**

**ج:** 
- ✅ **نعم** - يمكن استخدام هذه البيانات في حساب استحقاق الإجازات
- مثال: `إجازات مستحقة = (عدد الأيام الحالية / workDaysPerCycle) * leaveDaysPerCycle`

---

### **س10: هل النظام يدعم أنواع مختلفة من الدورات؟**

**ج:** 
- حالياً: يدعم **دورة واحدة** فقط لكل موظف
- مستقبلاً: يمكن توسيعه لدعم دورات متعددة (شتوي/صيفي مثلاً)

---

## 🔒 الصلاحيات والأمان

### **من يملك الصلاحية؟**

- ✅ **SUPER_ADMIN فقط**
- ❌ لا يمكن تعيين أدمن آخرين على هذه الوظيفة

### **Middleware المستخدم:**

```typescript
import { checkHRAccess } from '../../middleware/permissions.middleware.js'

workLeaveCycleHandler.use(checkHRAccess)
```

### **الحماية على مستوى قاعدة البيانات:**

- علامة `hasCustomCycle` تمنع الكتابة الخاطئة
- سجل التدقيق يحفظ كل التغييرات
- استعلامات محددة ودقيقة

---

## 🚀 الأداء والتحسينات

### **Indexes المقترحة:**

```sql
CREATE INDEX idx_employee_custom_cycle ON HR_Employee(hasCustomCycle);
CREATE INDEX idx_cycle_log_entity ON HR_CycleChangeLog(entityType, entityId);
CREATE INDEX idx_cycle_log_date ON HR_CycleChangeLog(changedAt DESC);
```

### **Pagination:**

- ✅ 8 موظفين لكل صفحة
- ✅ استعلام `count()` منفصل لحساب إجمالي النتائج
- ✅ استخدام `skip` و `take` لتحسين الأداء

---

## 📅 التحديثات المستقبلية المقترحة

| الميزة | الأولوية | الحالة |
|--------|---------|--------|
| 📊 تقرير سجل التغييرات | متوسطة | 📋 مقترح |
| 🔄 دورات موسمية (شتوي/صيفي) | منخفضة | 📋 مقترح |
| 📈 رسوم بيانية للدورات | منخفضة | 📋 مقترح |
| 🔗 ربط مباشر بنظام الإجازات | عالية | 📋 مقترح |
| 📱 إشعارات عند تغيير الدورة | متوسطة | 📋 مقترح |

---

## 🎓 الخلاصة

نظام **إدارة دورة العمل والإجازات** هو:

✅ **شامل** - يغطي جميع الحالات  
✅ **مرن** - يسمح بالتخصيص على مستوى الوظيفة والموظف  
✅ **آمن** - حماية من التعديلات الخاطئة  
✅ **شفاف** - سجل كامل لكل التغييرات  
✅ **عملي** - تقارير جاهزة للاستخدام  

---

## 📞 للدعم

لأي استفسارات أو اقتراحات حول النظام، يرجى التواصل مع فريق التطوير.

---

**آخر تحديث:** 4 نوفمبر 2025  
**الإصدار:** 1.0.0  
**الحالة:** ✅ جاهز للإنتاج
