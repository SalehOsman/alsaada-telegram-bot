# 📋 إدارة العاملين - التوثيق الكامل

## 🎯 نظرة عامة

نظام إدارة العاملين هو حجر الزاوية في منصة إدارة الموارد البشرية. يوفر النظام إدارة متكاملة لجميع بيانات الموظفين من لحظة التعيين وحتى انتهاء الخدمة، مع ربط سلس بجميع الأنظمة الفرعية (الإجازات، الرواتب، العقوبات، الإيقافات).

### ✨ المميزات الرئيسية

- 🎯 **إدارة متكاملة:** تسجيل ومتابعة كامل بيانات الموظف
- 🔄 **تكامل ذكي:** ربط مباشر مع أنظمة الرواتب والإجازات والعقوبات
- 📊 **تتبع الحالات:** 8 حالات توظيف مختلفة مع معالجة خاصة
- 📸 **المرفقات:** رفع وحفظ صور بطاقات الرقم القومي
- 🔍 **بحث متقدم:** تصفية وبحث بمعايير متعددة
- 📈 **تقارير Excel:** تصدير احترافي للبيانات

---

## 📂 الملفات المرتبطة

| الملف | الوصف | الحجم |
|------|-------|--------|
| `employees-list.handler.ts` | القائمة الرئيسية والخيارات | ~60 سطر |
| `employees-add.handler.ts` | ⭐ نموذج إضافة موظف (8 خطوات) | ~820 سطر |
| `employee-edit.handler.ts` | تعديل بيانات الموظف | ~450 سطر |
| `employee-status-simple.handler.ts` | تغيير حالة الموظف | ~240 سطر |
| `employee-filters.handler.ts` | خيارات التصفية | ~170 سطر |
| `employee-filter-results.handler.ts` | عرض نتائج التصفية | ~340 سطر |
| `employee-details.handler.ts` | عرض البطاقة الكاملة | ~280 سطر |
| `employee-export.handler.ts` | تصدير إلى Excel | ~220 سطر |
| `employee-search.handler.ts` | البحث النصي | ~110 سطر |
| `employees-view-previous.handler.ts` | الموظفين السابقين | ~230 سطر |
| `employee-previous-filters.handler.ts` | تصفية السابقين | ~180 سطر |

**📊 الإجمالي:** 11 ملف | ~3,100 سطر برمجي

---

## 🎨 الوظائف الرئيسية

### **1️⃣ القائمة الرئيسية** (`employees-list.handler.ts`)

واجهة رئيسية للوصول السريع لجميع وظائف إدارة الموظفين.

#### **الخيارات المتاحة:**

**📊 للجميع:**
- 🏢 **حسب القسم** - عرض موظفي قسم معين
- 📍 **حسب المحافظة** - عرض حسب محافظة الإقامة
- 💼 **حسب الوظيفة** - عرض حسب المسمى الوظيفي
- 📊 **حسب حالة العمل** - تصفية حسب الحالة (نشط/موقوف/إلخ)
- 👥 **عرض جميع العاملين** - قائمة كاملة بترقيم
- 📥 **تصدير الكل Excel** - تصدير شامل للبيانات

**🔐 للأدمن فقط (ADMIN+):**
- ➕ **إضافة عامل جديد** - نموذج شامل لتسجيل موظف
- 📂 **عرض العاملين السابقين** - المستقيلين/المفصولين/المتقاعدين

#### **Callback Patterns:**
```typescript
'employeesListHandler'
'menu:sub:hr-management:employees-list'
```

---

### **2️⃣ إضافة موظف جديد** ⭐ (`employees-add.handler.ts`)

**الحجم:** 820+ سطر  
**الأهمية:** ⭐⭐⭐⭐⭐  
**النوع:** Multi-Step Form (8 خطوات)

#### **خطوات النموذج المتطور:**

##### **📝 الخطوة 1: الاسم الكامل**
```
� المطلوب: إدخال الاسم الكامل للموظف
✅ التحقق: صحة الاسم (حروف عربية/إنجليزية + مسافات)
❌ رفض: الأسماء غير الصحيحة أو الرموز
```

##### **📛 الخطوة 2: اسم الشهرة**
```
� المطلوب: إدخال اسم الشهرة
🤖 توليد تلقائي: استخراج الاسم الأول من الاسم الكامل
⏭️ تخطي: سيُولّد تلقائياً إذا لم يُدخل
```

##### **💼 الخطوة 3: اختيار الوظيفة**
```
� العرض: قائمة جميع الوظائف النشطة (isActive = true)
🏢 التفاصيل: عرض القسم التابع لكل وظيفة
📊 الترتيب: حسب orderIndex ثم الاسم
🎯 الأهمية: تحديد القسم والراتب تلقائياً
```

##### **🆔 الخطوة 4: الرقم القومي** (ذكية ⭐)
```
🔹 المطلوب: 14 رقم
✅ التحقق الذكي:
   - صحة الصيغة (14 رقم)
   - استخراج تاريخ الميلاد تلقائياً
   - تحديد الجنس تلقائياً
   - استنتاج المحافظة من الكود
🚫 منع التكرار: التحقق من عدم تسجيل الرقم مسبقاً
📊 استخراج البيانات:
   - تاريخ الميلاد (YYYY-MM-DD)
   - الجنس (ذكر/أنثى)
   - المحافظة (من رمز المحافظة)
```

**دالة التحقق:**
```typescript
validateNationalIDWithInfo(text: string): {
  isValid: boolean
  birthDate: string      // "1995-01-01"
  gender: 'male' | 'female'
  governorateCode: string  // "01" => Cairo
}
```

##### **📱 الخطوة 5: رقم الموبايل** (ذكية ⭐)
```
� المطلوب: 11 رقم (01xxxxxxxxx)
✅ التحقق الذكي:
   - صحة الصيغة المصرية
   - تحديد الشبكة تلقائياً
📡 الشبكات المدعومة:
   - 🔴 فودافون (010, 011, 015)
   - 🟠 أورانج (012)
   - 🟢 اتصالات (011, 014)
   - 🟡 وي (015)
```

**دالة التحقق:**
```typescript
validateEgyptPhoneWithInfo(text: string): {
  isValid: boolean
  formatted: string       // "01012345678"
  operator: 'Vodafone' | 'Orange' | 'Etisalat' | 'WE'
}
```

##### **📍 الخطوة 6: محافظة الإقامة**
```
� العرض: قائمة 27 محافظة مصرية
� الترتيب: حسب orderIndex (القاهرة، الجيزة، الإسكندرية...)
🎨 التنسيق: عرض في صفين (Inline Keyboard)
```

##### **📅 الخطوة 7: تاريخ بدء العمل**
```
🔹 العرض: تقويم تفاعلي (Calendar)
✅ التحقق: عدم كون التاريخ في المستقبل
🔄 التنسيق: عربي كامل (7 نوفمبر 2025)
```

##### **📸 الخطوة 8: صور بطاقة الرقم القومي** (اختيارية)
```
🔹 المطلوب:
   1. الوجه الأمامي للبطاقة (اختياري)
   2. الوجه الخلفي للبطاقة (اختياري)
⏭️ التخطي: يمكن تخطي كلا الصورتين
💾 الحفظ: في مجلد مخصص منظم

📁 مسار الحفظ:
attachments/
  └── employees/
      └── {employeeCode}/          # مثال: ENG-001
          └── id-cards/
              ├── front_2025-11-07_143025.jpg
              └── back_2025-11-07_143045.jpg
```

#### **ملخص المراجعة:**

```markdown
📋 مراجعة البيانات قبل الحفظ

📝 البيانات الأساسية:
👤 الاسم: محمد أحمد علي
📛 الشهرة: محمد
💼 الوظيفة: مهندس برمجيات
📋 القسم: تقنية المعلومات
🆔 الرقم القومي: 29501012345678
📅 تاريخ الميلاد: 1995-01-01
🎂 العمر: 30 سنة
⚧️ الجنس: ذكر
📍 محافظة الإقامة: القاهرة

📞 معلومات الاتصال:
📱 الموبايل: 01012345678
📡 الشبكة: فودافون

💼 معلومات العمل:
📅 تاريخ بدء العمل: 2025-01-01

━━━━━━━━━━━━━━━━━━━━
هل تريد حفظ هذه البيانات؟
```


#### **💾 عملية الحفظ المتقدمة:**

النظام يقوم بالعمليات التالية بشكل تلقائي:

##### **1. توليد كود الموظف الفريد:**
```typescript
const codeInfo = await EmployeeCodeManager.generateEmployeeCode(positionId)
// نتيجة: { employeeCode: "ENG-001", positionCode: "ENG", sequenceNumber: 1 }
```

##### **2. حساب العمر الدقيق:**
```typescript
let age = todayDate.getFullYear() - birthDate.getFullYear()
const monthDiff = todayDate.getMonth() - birthDate.getMonth()
if (monthDiff < 0 || (monthDiff === 0 && todayDate.getDate() < birthDate.getDate())) {
  age-- // تعديل العمر إذا لم يحل عيد الميلاد بعد
}
```

##### **3. حفظ الصور بشكل منظم:**
```typescript
// الوجه الأمامي
const frontAttachment = await AttachmentsManager.saveIdCard(
  employeeCode,     // "ENG-001"
  fullName,         // "محمد أحمد علي"
  imageBuffer,      // Buffer من Telegram
  'id-card-front.jpg',
  'front'
)

// الوجه الخلفي
const backAttachment = await AttachmentsManager.saveIdCard(
  employeeCode,
  fullName,
  imageBuffer,
  'id-card-back.jpg',
  'back'
)
```

##### **4. إنشاء سجل الموظف في قاعدة البيانات:**
```typescript
const employee = await prisma.employee.create({
  data: {
    // بيانات أساسية
    employeeCode,              // "ENG-001"
    fullName,                  // "محمد أحمد علي"
    nickname,                  // "محمد"
    nationalId,                // "29501011234567"
    gender,                    // "male"
    dateOfBirth,               // 1995-01-01
    age,                       // 30
    
    // معلومات الاتصال
    personalPhone,             // "01012345678"
    phoneOperator,             // "Vodafone"
    governorateId,             // رقم محافظة الإقامة
    
    // معلومات العمل
    departmentId,              // من الوظيفة المختارة
    positionId,                // الوظيفة
    hireDate,                  // تاريخ التعيين
    employmentStatus: 'ACTIVE', // نشط افتراضياً
    
    // المرفقات
    nationalIdCardUrl,         // مسار نسبي للصور
    
    // القيم الافتراضية
    isActive: true,
    createdAt: new Date(),
    updatedAt: new Date()
  }
})
```

##### **5. تعيين القيم الافتراضية للإجازات:**
```typescript
// جلب القيم الافتراضية من الوظيفة
const position = await prisma.position.findUnique({
  where: { id: positionId },
  select: {
    defaultWorkDaysPerCycle: true,    // مثال: 25 يوم
    defaultLeaveDaysPerCycle: true    // مثال: 10 أيام
  }
})

// تطبيق على الموظف
await prisma.employee.update({
  where: { id: employee.id },
  data: {
    workDaysPerCycle: position.defaultWorkDaysPerCycle,
    leaveDaysPerCycle: position.defaultLeaveDaysPerCycle,
    // القيم القياسية للإجازات
    annualLeaveBalance: 21,     // 21 يوم إجازة سنوية
    sickLeaveBalance: 180,      // 180 يوم إجازة مرضية
    casualLeaveBalance: 7       // 7 أيام إجازة عارضة
  }
})
```

##### **6. إرسال تقارير للمسؤولين:**
```typescript
// جلب جميع المسؤولين النشطين (عدا المنفّذ)
const admins = await prisma.user.findMany({
  where: {
    role: { in: ['SUPER_ADMIN', 'ADMIN'] },
    isActive: true,
    NOT: { telegramId: currentUserId }
  }
})

// إرسال تقرير لكل مسؤول
for (const admin of admins) {
  await ctx.api.sendMessage(admin.telegramId, `
    🎉 تم إضافة موظف جديد!
    
    👤 ${fullName} (${employeeCode})
    💼 ${positionTitle}
    📅 ${formatDate(hireDate)}
    
    بواسطة: ${currentUserName}
  `)
}
```

##### **7. تنظيف الرسائل المؤقتة:**
```typescript
// حذف جميع رسائل النموذج للحفاظ على نظافة المحادثة
await deleteAllMessagesBetween(
  ctx,
  startMessageId,  // أول رسالة في النموذج
  endMessageId     // آخر رسالة في النموذج
)
```

---

### **3️⃣ تعديل بيانات الموظف** (`employee-edit.handler.ts`)

نظام تعديل مرن يسمح بتحديث معظم بيانات الموظف مع حفظ سجل التغييرات.

#### **الحقول القابلة للتعديل:**

| الحقل | النوع | ملاحظات |
|-------|------|---------|
| 👤 الاسم الكامل | نص | مع التحقق من الصحة |
| 📛 اسم الشهرة | نص | يمكن توليده تلقائياً |
| 📱 رقم الموبايل | رقم | مع تحديد الشبكة |
| 📍 محافظة الإقامة | اختيار | من قائمة المحافظات |
| 🏠 العنوان | نص | عنوان تفصيلي |
| 💼 الوظيفة | اختيار | يحدث القسم تلقائياً |
| 💰 الراتب الأساسي | رقم | للمسؤولين فقط |
| 🏖️ أيام دورة العمل | رقم | يؤثر على الإجازات |
| 🏖️ أيام دورة الإجازة | رقم | يؤثر على الإجازات |

#### **المميزات الخاصة:**

✅ **تسجيل التغييرات:**
- حفظ القيم القديمة
- تسجيل المستخدم المعدّل
- تسجيل التاريخ والوقت

✅ **التحقق من الصلاحيات:**
- SUPER_ADMIN: تعديل كل شيء
- ADMIN: تعديل البيانات الأساسية فقط
- غير المصرح: رفض التعديل

✅ **الإشعارات:**
- إشعار للمستخدم المنفّذ
- إشعار لجميع المسؤولين
- تفاصيل التغييرات

---

### **4️⃣ تغيير حالة الموظف** (`employee-status-simple.handler.ts`)

نظام متقدم لإدارة دورة حياة الموظف من التعيين إلى انتهاء الخدمة.

#### **الحالات الثمانية المدعومة:**

```typescript
enum EmploymentStatus {
  ACTIVE        = 'نشط'           // يعمل بشكل طبيعي
  ON_LEAVE      = 'في إجازة'      // في إجازة رسمية
  SUSPENDED     = 'موقوف'         // موقوف عن العمل (بدون راتب)
  RESIGNED      = 'مستقيل'        // قدم استقالة
  TERMINATED    = 'مفصول'         // تم فصله
  RETIRED       = 'متقاعد'        // بلغ سن التقاعد
  ON_MISSION    = 'في مأمورية'    // في مهمة خارجية
  SETTLED       = 'مسوّى'         // تمت تسوية حسابه
}
```

#### **معالجة خاصة لكل حالة:**

##### **🔴 SUSPENDED (موقوف):**
```typescript
// يتم إيقاف الراتب تلقائياً
// يسجل تاريخ الإيقاف
// يمنع من الإجازات والمعاملات
// لا يظهر في تقارير الموظفين النشطين
```

##### **📤 RESIGNED (مستقيل):**
```typescript
// طلب تاريخ الاستقالة
data.resignationDate = selectedDate
// تحديث حالة isActive = false
// نقل إلى قائمة السابقين
// حساب فترة الخدمة
```

##### **🚫 TERMINATED (مفصول):**
```typescript
// طلب تاريخ الفصل
data.terminationDate = selectedDate
// تحديث حالة isActive = false
// تسجيل سبب الفصل (اختياري)
// إيقاف جميع المستحقات
```

##### **💼 SETTLED (مسوّى):**
```typescript
// التحقق من:
// - عدم وجود سلف معلقة
// - عدم وجود عقوبات غير منفذة
// - عدم وجود إجازات مفتوحة
// تسجيل تاريخ التسوية
// إصدار تقرير التسوية النهائي
```

---

### **5️⃣ التصفية والبحث المتقدم**

#### **أ) التصفية** (`employee-filters.handler.ts`)


**أنواع التصفية المتاحة:**

**1. 🏢 حسب القسم:**
```typescript
const departments = await prisma.department.findMany({
  where: { isActive: true },
  orderBy: { name: 'asc' },
  include: {
    _count: {
      select: { employees: true }
    }
  }
})
// عرض: "تقنية المعلومات (25 موظف)"
```

**2. 📍 حسب المحافظة:**
```typescript
const governorates = await prisma.governorate.findMany({
  orderBy: { orderIndex: 'asc' },
  include: {
    _count: {
      select: { employees: true }
    }
  }
})
// عرض: "القاهرة (150 موظف)"
```

**3. 💼 حسب الوظيفة:**
```typescript
const positions = await prisma.position.findMany({
  where: { isActive: true },
  include: {
    department: true,
    _count: {
      select: { employees: true }
    }
  },
  orderBy: { titleAr: 'asc' }
})
// عرض: "مهندس برمجيات - تقنية المعلومات (12 موظف)"
```

**4. 📊 حسب حالة العمل:**
```markdown
الحالات الثمانية:
✅ نشط (ACTIVE)
🏖️ في إجازة (ON_LEAVE)
🔴 موقوف (SUSPENDED)
📤 مستقيل (RESIGNED)
🚫 مفصول (TERMINATED)
👴 متقاعد (RETIRED)
✈️ في مأمورية (ON_MISSION)
💼 مسوّى (SETTLED)
```

---

#### **ب) عرض نتائج التصفية** (`employee-filter-results.handler.ts`)

**نظام الصفحات:**
- 📄 عرض **10 موظفين** في كل صفحة
- ⬅️➡️ أزرار تنقل بين الصفحات
- 📊 إحصائيات إجمالية
- 🔢 ترقيم تسلسلي

**مثال على العرض:**
```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━
🏢 موظفو قسم: تقنية المعلومات
━━━━━━━━━━━━━━━━━━━━━━━━━━

👥 العدد الإجمالي: 25 موظف
📄 الصفحة: 1 من 3 (عرض 1-10)

━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ محمد أحمد علي (ENG-001)
   💼 مهندس برمجيات
   ✅ نشط | 📅 2023-01-15

2️⃣ أحمد حسن محمود (ENG-002)
   💼 مهندس برمجيات
   ✅ نشط | 📅 2023-03-20

3️⃣ سارة علي إبراهيم (ENG-003)
   💼 مهندسة قواعد بيانات
   ✅ نشط | 📅 2023-05-10

... (7 موظفين آخرين)

━━━━━━━━━━━━━━━━━━━━━━━━━━
⬅️ الصفحة التالية | العودة 🔙
```

---

#### **ج) البحث النصي** (`employee-search.handler.ts`)

**معايير البحث المتعددة:**

```typescript
const employees = await prisma.employee.findMany({
  where: {
    OR: [
      { fullName: { contains: searchText, mode: 'insensitive' } },
      { nickname: { contains: searchText, mode: 'insensitive' } },
      { employeeCode: { contains: searchText, mode: 'insensitive' } },
      { nationalId: { contains: searchText } },
      { personalPhone: { contains: searchText } }
    ],
    isActive: true
  },
  include: {
    position: { include: { department: true } },
    governorate: true
  },
  orderBy: { fullName: 'asc' }
})
```

**مميزات البحث:**
- 🔍 بحث غير حساس لحالة الأحرف
- 🔢 بحث جزئي (يدعم الكلمات الجزئية)
- 🎯 بحث في 5 حقول مختلفة
- ⚡ نتائج فورية
- 📊 عرض تفاصيل كاملة

---

### **6️⃣ عرض بطاقة الموظف الكاملة** (`employee-details.handler.ts`)

بطاقة شاملة تعرض جميع معلومات الموظف بتنسيق احترافي.

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━
👤 بطاقة الموظف
━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 البيانات الأساسية
━━━━━━━━━━━━━━━━━━━━━━━━━━
🆔 كود الموظف: ENG-001
👤 الاسم الكامل: محمد أحمد علي حسن
📛 اسم الشهرة: محمد
🆔 الرقم القومي: 29501011234567
📅 تاريخ الميلاد: 1995-01-01
🎂 العمر: 30 سنة و 3 أشهر
⚧️ الجنس: ذكر
📍 محافظة الإقامة: القاهرة

━━━━━━━━━━━━━━━━━━━━━━━━━━
💼 معلومات العمل
━━━━━━━━━━━━━━━━━━━━━━━━━━
💼 الوظيفة: مهندس برمجيات
🏢 القسم: تقنية المعلومات
📅 تاريخ التعيين: 2023-01-15
⏱️ مدة الخدمة: 2 سنة و 10 أشهر
✅ الحالة: نشط

━━━━━━━━━━━━━━━━━━━━━━━━━━
💰 المعلومات المالية
━━━━━━━━━━━━━━━━━━━━━━━━━━
💵 الراتب الأساسي: 5,000 جنيه
➕ البدلات الثابتة: 500 جنيه
💰 إجمالي الراتب: 5,500 جنيه

━━━━━━━━━━━━━━━━━━━━━━━━━━
📞 معلومات الاتصال
━━━━━━━━━━━━━━━━━━━━━━━━━━
📱 الموبايل: 01012345678
📡 الشبكة: فودافون 🔴
🏠 العنوان: شارع النصر، المعادي، القاهرة

━━━━━━━━━━━━━━━━━━━━━━━━━━
🏖️ معلومات الإجازات
━━━━━━━━━━━━━━━━━━━━━━━━━━
📅 رصيد الإجازات السنوية: 18 يوم
🏥 رصيد الإجازات المرضية: 175 يوم
🚨 رصيد الإجازات العارضة: 6 أيام

🔄 نظام دورة العمل/الإجازة:
   ⚓ 25 يوم عمل + 10 أيام إجازة
   📅 الإجازة القادمة المتوقعة: 2025-12-15

━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**الخيارات المتاحة بعد العرض:**
- ✏️ **تعديل البيانات** - فتح نموذج التعديل
- 🔄 **تغيير الحالة** - تحديث حالة العمل
- 🏖️ **عرض الإجازات** - تاريخ جميع الإجازات
- 💰 **عرض السلف** - السلف والمسحوبات
- 💵 **كشوف الرواتب** - تاريخ الرواتب
- � **العقوبات** - سجل العقوبات
- �📊 **التقارير** - تقارير مفصلة

---

### **7️⃣ تصدير Excel احترافي** (`employee-export.handler.ts`)

تصدير شامل لبيانات الموظفين بتنسيق Excel احترافي.

**الأعمدة المُصدَّرة:**

| # | العمود | النوع | التنسيق |
|---|--------|------|---------|
| 1 | كود الموظف | نص | ENG-001 |
| 2 | الاسم الكامل | نص | محمد أحمد علي |
| 3 | اسم الشهرة | نص | محمد |
| 4 | الرقم القومي | نص | 29501011234567 |
| 5 | الوظيفة | نص | مهندس برمجيات |
| 6 | القسم | نص | تقنية المعلومات |
| 7 | الراتب الأساسي | رقم | ٥,٠٠٠ جنيه |
| 8 | تاريخ التعيين | تاريخ | ٢٠٢٣-٠١-١٥ |
| 9 | مدة الخدمة | نص | ٢ سنة و ١٠ أشهر |
| 10 | الحالة | نص | نشط ✅ |
| 11 | رقم الموبايل | نص | ٠١٠١٢٣٤٥٦٧٨ |
| 12 | المحافظة | نص | القاهرة |
| 13 | العمر | رقم | ٣٠ سنة |
| 14 | الجنس | نص | ذكر |

**المميزات:**
- ✅ تنسيق عربي كامل
- 🔢 أرقام عربية (هندية)
- 📅 تواريخ منسقة
- 🎨 ألوان وتنسيق احترافي
- 💾 حجم ملف محسّن

**اسم الملف:**
```
employees_2025-11-07_143025.xlsx
```

---

### **8️⃣ الموظفين السابقين** (`employees-view-previous.handler.ts`)

نظام متخصص لعرض وإدارة الموظفين الذين انتهت خدمتهم.

**الحالات المعروضة:**

```typescript
const previousStatuses = ['RESIGNED', 'TERMINATED', 'RETIRED', 'SETTLED']
```

| الحالة | الوصف | الرمز |
|--------|-------|------|
| RESIGNED | قدم استقالة رسمية | 📤 |
| TERMINATED | تم فصله من العمل | 🚫 |
| RETIRED | بلغ سن التقاعد | 👴 |
| SETTLED | تمت تسويته نهائياً | 💼 |

**المعلومات الإضافية:**
- 📅 تاريخ انتهاء الخدمة
- ⏱️ إجمالي مدة الخدمة
- 💰 آخر راتب مستحق
- ✅ حالة التسوية (مسوّى / غير مسوّى)
- 📋 سبب الانتهاء

**الفلاتر المتاحة:**
- حسب الحالة (مستقيل/مفصول/متقاعد/مسوّى)
- حسب القسم السابق
- حسب تاريخ انتهاء الخدمة (شهر/سنة)

---

## �️ الأدوات المساعدة (Utilities)

### **1. EmployeeCodeManager**

مدير توليد أكواد الموظفين الفريدة بشكل تلقائي.

```typescript
class EmployeeCodeManager {
  /**
   * توليد كود موظف فريد بناءً على الوظيفة
   * @param positionId - معرّف الوظيفة
   * @returns كود الموظف والمعلومات ذات الصلة
   */
  static async generateEmployeeCode(positionId: number): Promise<{
    employeeCode: string    // "ENG-001"
    positionCode: string    // "ENG"
    sequenceNumber: number  // 1
  }>
}
```

**آلية العمل:**
```typescript
// 1. جلب كود الوظيفة
const position = await prisma.position.findUnique({
  where: { id: positionId },
  select: { positionCode: true }  // "ENG"
})

// 2. حساب آخر رقم تسلسلي
const lastEmployee = await prisma.employee.findFirst({
  where: {
    employeeCode: { startsWith: position.positionCode }
  },
  orderBy: { employeeCode: 'desc' }
})

// 3. توليد الكود الجديد
const sequenceNumber = lastEmployee ? parseInt(lastEmployee.employeeCode.split('-')[1]) + 1 : 1
const employeeCode = `${position.positionCode}-${sequenceNumber.toString().padStart(3, '0')}`

// نتيجة: "ENG-001", "ENG-002", ...
```

---

### **2. AttachmentsManager**

مدير حفظ وتنظيم المرفقات والصور.

```typescript
class AttachmentsManager {
  /**
   * حفظ صورة بطاقة الرقم القومي
   */
  static async saveIdCard(
    employeeCode: string,    // "ENG-001"
    employeeName: string,    // "محمد أحمد"
    imageBuffer: Buffer,     // بيانات الصورة من Telegram
    filename: string,        // "id-card-front.jpg"
    type: 'front' | 'back'   // نوع الوجه
  ): Promise<{ filePath: string }>
  
  /**
   * تحويل المسار الكامل إلى مسار نسبي
   */
  static getRelativeFilePath(fullPath: string): string
}
```

**مثال الاستخدام:**
```typescript
const attachment = await AttachmentsManager.saveIdCard(
  'ENG-001',
  'محمد أحمد علي',
  photoBuffer,
  'id-card-front.jpg',
  'front'
)

// النتيجة:
// {
//   filePath: "attachments/employees/ENG-001/id-cards/front_2025-11-07_143025.jpg"
// }
```

---

### **3. generateNickname**

دالة توليد اسم الشهرة من الاسم الكامل.

```typescript
/**
 * استخراج اسم الشهرة (الاسم الأول) من الاسم الكامل
 * @param fullName - الاسم الكامل
 * @returns اسم الشهرة
 */
function generateNickname(fullName: string): string {
  return fullName.trim().split(' ')[0]
}
```

**أمثلة:**
```typescript
generateNickname('محمد أحمد علي حسن')     // => 'محمد'
generateNickname('فاطمة حسن إبراهيم')     // => 'فاطمة'
generateNickname('Ahmed Mohamed Ali')    // => 'Ahmed'
```

---

## 📊 إحصائيات النظام

| المقياس | القيمة |
|---------|--------|
| 📁 عدد الملفات | **11 ملف** |
| 📝 إجمالي الأسطر البرمجية | **~3,100 سطر** |
| 🎯 عدد الوظائف الرئيسية | **8 وظائف** |
| 🔧 عدد الوظائف الفرعية | **25+ وظيفة** |
| 📊 الحقول القابلة للتعديل | **15 حقل** |
| 📸 أنواع المرفقات | **2 نوع** |
| 🔍 معايير البحث | **5 معايير** |
| 📋 حالات التوظيف | **8 حالات** |

---

## 🔐 نظام الصلاحيات

### **قراءة البيانات:**

| الدور | الصلاحية | الوصف |
|-------|----------|--------|
| SUPER_ADMIN | ✅ كامل | الوصول لجميع البيانات |
| ADMIN | ✅ محدود | الموظفين المعينين على الوظيفة فقط |
| USER | ❌ ممنوع | لا يمكن القراءة |

### **إضافة/تعديل الموظفين:**

| الدور | الصلاحية | القيود |
|-------|----------|--------|
| SUPER_ADMIN | ✅ كامل | بدون قيود |
| ADMIN | ✅ محدود | إذا كان معيناً على الوظيفة |
| USER | ❌ ممنوع | لا يمكن الإضافة/التعديل |

### **حذف الموظفين:**

| الدور | الصلاحية |
|-------|----------|
| SUPER_ADMIN | ✅ نعم |
| ADMIN | ❌ لا |
| USER | ❌ لا |

### **تغيير حالة الموظف:**

| الدور | الصلاحية | القيود |
|-------|----------|--------|
| SUPER_ADMIN | ✅ كامل | جميع الحالات |
| ADMIN | ⚠️ محدود | حالات معينة فقط |
| USER | ❌ ممنوع | لا يمكن التغيير |

---

## 🔗 التكامل مع الأنظمة الأخرى

يتكامل نظام إدارة الموظفين بشكل وثيق مع:

- 💰 **نظام الرواتب:** حساب الرواتب الشهرية/اليومية
- 🏖️ **نظام الإجازات:** إدارة جميع أنواع الإجازات
- 🚫 **نظام العقوبات:** تطبيق العقوبات والخصومات
- 🔴 **نظام الإيقافات:** إيقاف عن العمل وإيقاف الراتب
- 💼 **نظام السلف:** السلف والمسحوبات
- 📊 **نظام التقارير:** تقارير شاملة ومفصلة
- 🔔 **نظام الإشعارات:** إشعارات تلقائية للمسؤولين

---

**📅 آخر تحديث:** 7 نوفمبر 2025  
**📦 الإصدار:** 3.0.0  
**✅ الحالة:** مكتمل ومحدث
