# 🔐 نظام الصلاحيات والأدوار - دليل شامل

> **تاريخ الإنشاء:** 29 أكتوبر 2025  
> **الإصدار:** 1.0  
> **الحالة:** قيد التنفيذ  

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [الأدوار الأساسية](#الأدوار-الأساسية)
3. [مفهوم minRole](#مفهوم-minrole)
4. [التعيينات](#التعيينات)
5. [القواعد الأساسية](#القواعد-الأساسية)
6. [أمثلة عملية](#أمثلة-عملية)
7. [المشاكل الموجودة](#المشاكل-الموجودة)
8. [خطة الإصلاح](#خطة-الإصلاح)

---

## 🎯 نظرة عامة

### الفلسفة الأساسية

نظام الصلاحيات في البوت مبني على **3 مستويات**:

```
┌─────────────────────────────────────────┐
│         المستوى 1: الأدوار              │
│         (Role-Based Access)             │
├─────────────────────────────────────────┤
│         المستوى 2: الحد الأدنى          │
│         (minRole for Departments)       │
├─────────────────────────────────────────┤
│         المستوى 3: التعيينات            │
│    (DepartmentAdmin & SubFeatureAdmin)  │
└─────────────────────────────────────────┘
```

### المبدأ الأساسي

> **السوبر أدمن (SUPER_ADMIN) يرى كل شيء في البوت**  
> **الزائر (GUEST) لا يرى شيء إلا طلب الانضمام**

---

## 👥 الأدوار الأساسية

### التعريف في قاعدة البيانات

```prisma
enum Role {
  SUPER_ADMIN    // المستوى 4
  ADMIN          // المستوى 3
  MODERATOR      // المستوى 2
  USER           // المستوى 2
  GUEST          // المستوى 1
}
```

### الهرمية (ROLE_HIERARCHY)

```typescript
export const ROLE_HIERARCHY: Record<Role, number> = {
  SUPER_ADMIN: 4,  // 👑 الملك
  ADMIN: 3,        // 👨‍💼 المدير
  MODERATOR: 2,    // 🧑‍💻 المشرف
  USER: 2,         // 👤 الموظف
  GUEST: 1,        // 🚶 الزائر
}
```

### الوصف التفصيلي

#### 1️⃣ SUPER_ADMIN - السوبر أدمن 👑

**الصلاحيات:**
- ✅ يرى **جميع** الأقسام في البوت
- ✅ يدخل على **جميع** الوظائف
- ✅ يتجاوز **كل** القيود
- ✅ الوحيد الذي يحدد `minRole` للأقسام والوظائف

**الكود:**
```typescript
if (userContext.role === 'SUPER_ADMIN') {
  return true  // دائماً
}
```

---

#### 2️⃣ ADMIN - الأدمن 👨‍💼

**الصلاحيات:**
- ✅ يمكن تعيينه على الأقسام
- ✅ يمكن تعيينه على الوظائف
- ⚠️ يخضع لقيود `minRole`

**مثال:**
```
قسم: minRole = ADMIN
✅ الأدمن يقدر يدخل (دوره >= minRole)

قسم: minRole = SUPER_ADMIN
❌ الأدمن ما يقدرش يدخل (دوره < minRole)
```

---

#### 3️⃣ MODERATOR - المشرف 🧑‍💻

**الصلاحيات:**
- ⚠️ محدودة أكثر من ADMIN
- ✅ يمكن تعيينه على أقسام ذات `minRole = MODERATOR` أو أقل
- ❌ لا يمكن تعيينه على أقسام ذات `minRole = ADMIN` أو `SUPER_ADMIN`

---

#### 4️⃣ USER - المستخدم 👤

**الصلاحيات:**
- ✅ يمكن تعيينه على أقسام ذات `minRole = USER`
- ❌ لا يمكن تعيينه على أقسام ذات `minRole` أعلى

---

#### 5️⃣ GUEST - الزائر 🚶

**الصلاحيات:**
- ❌ **لا يرى أي شيء** في البوت
- ❌ لا يمكن الوصول للقائمة الرئيسية
- ✅ فقط يمكنه **طلب الانضمام للبوت**

**الكود:**
```typescript
if (!ctx.dbUser || userRole === 'GUEST') {
  await ctx.reply('⛔ يجب عليك تقديم طلب انضمام أولاً')
  return
}
```

---

## 🚪 مفهوم minRole

### التعريف

**minRole** = **الحد الأدنى** من الدور المطلوب للوصول إلى قسم أو وظيفة.

### قاعدة البيانات

```prisma
model DepartmentConfig {
  code        String  @unique
  name        String
  minRole     String  @default("ADMIN")  // ← الحد الأدنى
  isEnabled   Boolean @default(true)
}

model SubFeatureConfig {
  code        String  @unique
  minRole     String?  // NULL = ترث من القسم الأب
}
```

### القاعدة الذهبية

```
minRole القسم = أقل درجة يمكنها الدخول

⚠️ لا يمكن تعيين شخص أقل من minRole
```

---

## 🎯 أمثلة على minRole

### مثال 1: قسم سري (SUPER_ADMIN فقط)

```typescript
DepartmentConfig {
  code: "top-secret",
  name: "القسم السري",
  minRole: "SUPER_ADMIN"
}
```

**النتيجة:**
- ✅ SUPER_ADMIN فقط يمكنه الدخول
- ❌ ADMIN لا يمكنه (حتى لو عُيّن)
- ❌ MODERATOR لا يمكنه
- ❌ USER لا يمكنه

**القاعدة:**
> **عندما minRole = SUPER_ADMIN، لا يمكن تعيين أي شخص آخر على القسم**

---

### مثال 2: قسم إداري (ADMIN)

```typescript
DepartmentConfig {
  code: "hr-management",
  name: "شئون العاملين",
  minRole: "ADMIN"
}
```

**النتيجة:**
- ✅ SUPER_ADMIN يدخل (دائماً)
- ✅ ADMIN يدخل (دوره = minRole)
- ✅ يمكن تعيين ADMIN كـ DepartmentAdmin
- ❌ لا يمكن تعيين MODERATOR أو USER (دورهم < minRole)

**الوظائف الفرعية:**
```typescript
SubFeatureConfig {
  code: "hr:salaries",
  departmentCode: "hr-management",
  minRole: "SUPER_ADMIN"  // ← أعلى من القسم ✅
}

SubFeatureConfig {
  code: "hr:employees",
  departmentCode: "hr-management",
  minRole: "ADMIN"  // ← نفس القسم ✅
}

SubFeatureConfig {
  code: "hr:reports",
  departmentCode: "hr-management",
  minRole: "MODERATOR"  // ← أقل من القسم ❌ ممنوع!
}
```

**القاعدة:**
> **minRole الوظيفة الفرعية >= minRole القسم الأب**

---

### مثال 3: قسم عام (USER)

```typescript
DepartmentConfig {
  code: "general-info",
  name: "المعلومات العامة",
  minRole: "USER"
}
```

**النتيجة:**
- ✅ SUPER_ADMIN يدخل
- ✅ ADMIN يدخل
- ✅ MODERATOR يدخل
- ✅ USER يدخل
- ✅ يمكن تعيين أي شخص ماعدا GUEST

---

## 🎫 التعيينات (Assignments)

### الأنواع

#### 1️⃣ DepartmentAdmin - أدمن القسم

**المعنى:**
تعيين شخص **محدد** كمسؤول عن القسم **بالكامل**.

**قاعدة البيانات:**
```prisma
model DepartmentAdmin {
  id           Int
  departmentId Int
  userId       Int
  telegramId   BigInt
  assignedBy   BigInt
  isActive     Boolean @default(true)
}
```

**الصلاحيات:**
- ✅ يدخل على القسم كاملاً
- ✅ يرى **جميع** الوظائف الفرعية (حسب دوره)
- ⚠️ **يجب** أن يكون دوره >= minRole القسم

**مثال:**
```
القسم: minRole = ADMIN
المستخدم: USER

❌ لا يمكن تعيينه كـ DepartmentAdmin
   (لأن USER < ADMIN)

✅ لو تم ترقيته إلى ADMIN
   يمكن تعيينه كـ DepartmentAdmin
```

---

#### 2️⃣ SubFeatureAdmin - أدمن الوظيفة

**المعنى:**
تعيين شخص على **وظيفة محددة فقط** في القسم.

**قاعدة البيانات:**
```prisma
model SubFeatureAdmin {
  id            Int
  subFeatureId  Int
  userId        Int
  telegramId    BigInt
  assignedBy    BigInt
  isActive      Boolean @default(true)
}
```

**الصلاحيات:**
- ✅ يدخل على الوظيفة المحددة **فقط**
- ❌ لا يرى باقي الوظائف
- ⚠️ **يجب** أن يكون دوره >= minRole الوظيفة

**مثال:**
```
القسم: hr-management (minRole = ADMIN)
├── الموظفين (minRole = ADMIN)
├── السلف (minRole = ADMIN)
└── الرواتب (minRole = SUPER_ADMIN)

المستخدم: ADMIN
تعيينه كـ SubFeatureAdmin على "السلف"

النتيجة:
✅ يرى "السلف" فقط
❌ لا يرى "الموظفين"
❌ لا يرى "الرواتب"
```

---

## 📜 القواعد الأساسية

### القاعدة 1: الهرمية

```
SUPER_ADMIN > ADMIN > MODERATOR = USER > GUEST
```

### القاعدة 2: التدرج في الأقسام

```
minRole القسم <= minRole أي وظيفة فرعية

✅ قسم (ADMIN) → وظيفة (ADMIN)
✅ قسم (ADMIN) → وظيفة (SUPER_ADMIN)
❌ قسم (ADMIN) → وظيفة (MODERATOR)  ← ممنوع!
```

### القاعدة 3: التعيينات

```
لا يمكن تعيين شخص على قسم/وظيفة إلا إذا:
دور الشخص >= minRole القسم/الوظيفة

✅ قسم (ADMIN) + مستخدم (ADMIN) = يمكن التعيين
❌ قسم (ADMIN) + مستخدم (USER) = لا يمكن التعيين
```

### القاعدة 4: السوبر أدمن

```
SUPER_ADMIN يتجاوز كل القواعد
```

### القاعدة 5: الزائر

```
GUEST لا يرى شيء في البوت
```

---

## 💡 أمثلة عملية شاملة

### السيناريو الكامل

#### الشركة:
```
1️⃣ خالد    → SUPER_ADMIN
2️⃣ سارة    → ADMIN
3️⃣ أحمد    → MODERATOR
4️⃣ فاطمة   → USER
5️⃣ عمر     → GUEST
```

#### الأقسام:

##### قسم 1: شئون العاملين
```typescript
DepartmentConfig {
  code: "hr-management",
  minRole: "ADMIN"
}

SubFeatures:
├── قوائم العاملين (minRole: ADMIN)
├── السلف (minRole: ADMIN)
├── الإجازات (minRole: ADMIN)
└── الرواتب (minRole: SUPER_ADMIN)
```

##### قسم 2: التقارير
```typescript
DepartmentConfig {
  code: "reports",
  minRole: "MODERATOR"
}

SubFeatures:
├── التقارير العامة (minRole: MODERATOR)
├── التقارير المالية (minRole: ADMIN)
└── التقارير السرية (minRole: SUPER_ADMIN)
```

#### التعيينات:
```
- فاطمة (USER) معينة كـ SubFeatureAdmin على "السلف"
```

---

### جدول الصلاحيات

| الشخص | الدور | شئون العاملين | قوائم العاملين | السلف | الإجازات | الرواتب | التقارير | التقارير العامة | التقارير المالية | التقارير السرية |
|-------|------|---------------|---------------|-------|---------|---------|----------|---------------|-----------------|-----------------|
| **خالد** | SUPER_ADMIN | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **سارة** | ADMIN | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | ✅ | ✅ | ❌ |
| **أحمد** | MODERATOR | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| **فاطمة** | USER | ❌ | ❌ | ✅* | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **عمر** | GUEST | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

**الملاحظات:**
- ✅* = فاطمة ترى "السلف" لأنها **معينة** عليها (رغم أن دورها USER)
- ❌ = لا يمكن الوصول

---

## 🐛 المشاكل الموجودة في الكود الحالي

### المشكلة 1: التعيينات تتطلب minRole ❌

**الكود الحالي:**
```typescript
// في permission-service.ts - canAccessDepartment
const hasRequiredRole = this.hasMinRole(userContext, minRole as Role)
if (!hasRequiredRole) {
  return false  // ❌ يرفض هنا حتى لو معيّن!
}

// ثم يفحص التعيينات
const isDeptAdmin = await Database.prisma.departmentAdmin...
```

**المشكلة:**
الكود يفحص `minRole` **قبل** التعيينات، فيرفض المستخدم قبل أن يصل لفحص التعيين.

**المثال:**
```
قسم: minRole = ADMIN
مستخدم: USER (معيّن كـ DepartmentAdmin)

المتوقع: ✅ يدخل (لأنه معيّن)
الواقع: ❌ لا يدخل (لأن الكود يفحص minRole أولاً)
```

**الحل:**
تغيير ترتيب الفحص:
```typescript
// 1. فحص SUPER_ADMIN
if (role === 'SUPER_ADMIN') return true

// 2. فحص التعيينات (قبل minRole)
const isDeptAdmin = await ...
if (isDeptAdmin) return true

// 3. فحص minRole (آخر شيء)
return hasMinRole(userContext, minRole)
```

---

### المشكلة 2: لا يوجد فحص عند التعيين ❌

**الكود الحالي:**
```typescript
// في section-management.handler.ts
await Database.prisma.departmentAdmin.create({
  data: {
    userId: user.id,  // ❌ يقبل أي user حتى لو دوره أقل!
  }
})
```

**المشكلة:**
لا يوجد فحص يمنع تعيين USER على قسم له minRole = ADMIN.

**الحل المطلوب:**
```typescript
// قبل التعيين:
const department = await Database.prisma.departmentConfig.findUnique({
  where: { code: 'hr-management' }
})

const minRoleLevel = ROLE_HIERARCHY[department.minRole] || 0
const userRoleLevel = ROLE_HIERARCHY[user.role] || 0

if (userRoleLevel < minRoleLevel) {
  return "❌ لا يمكن تعيين هذا المستخدم (دوره أقل من minRole)"
}
```

---

### المشكلة 3: لا يوجد فحص للوظائف الفرعية ❌

**المشكلة:**
يمكن إنشاء وظيفة فرعية بـ minRole أقل من minRole القسم.

**مثال:**
```typescript
DepartmentConfig {
  code: "hr-management",
  minRole: "ADMIN"
}

SubFeatureConfig {
  code: "hr:reports",
  departmentCode: "hr-management",
  minRole: "USER"  // ❌ أقل من القسم! يجب منع هذا
}
```

**الحل:**
```typescript
// عند إنشاء/تعديل SubFeatureConfig:
const department = await Database.prisma.departmentConfig.findUnique({
  where: { code: departmentCode }
})

const deptLevel = ROLE_HIERARCHY[department.minRole] || 0
const subLevel = ROLE_HIERARCHY[newMinRole] || 0

if (subLevel < deptLevel) {
  return "❌ minRole الوظيفة لا يمكن أن تكون أقل من minRole القسم"
}
```

---

### المشكلة 4: MenuBuilder يفحص minRole قبل التعيينات ⚠️

**الكود الحالي:**
```typescript
// في menu-builder.ts - buildSubMenu
const minRole = subFeatureConfig?.minRole || 'ADMIN'
if (PermissionService.hasMinRole(userContext, minRole)) {
  accessibleSubFeatures.push(subFeature)
}
```

**المشكلة:**
MenuBuilder يفحص minRole قبل عرض الأزرار، مما يخفي الوظائف حتى عن المعيّنين.

**الحل:**
```typescript
// الترتيب الصحيح:
// 1. إذا SUPER_ADMIN → أضف الزر
// 2. إذا DepartmentAdmin → أضف الزر (بدون فحص minRole)
// 3. إذا SubFeatureAdmin → أضف الزر (بدون فحص minRole)
// 4. فحص minRole فقط إذا لم يكن معيّن
```

---

## 🔧 خطة الإصلاح

### المرحلة 1: إصلاح منطق الصلاحيات ⚡

**الأولوية:** 🔴 عالية جداً

#### 1.1 إصلاح `canAccessDepartment`

**الملف:** `src/modules/permissions/permission-service.ts`

**التعديل:**
```typescript
static async canAccessDepartment(
  userContext: UserPermissionContext,
  departmentCode: string,
): Promise<boolean> {
  // 1. SUPER_ADMIN دائماً
  if (userContext.role === 'SUPER_ADMIN') {
    return true
  }

  const department = await Database.prisma.departmentConfig.findUnique({
    where: { code: departmentCode },
  })

  if (!department || !department.isEnabled) {
    return false
  }

  // 2. فحص DepartmentAdmin (قبل minRole!)
  const isDeptAdmin = await Database.prisma.departmentAdmin.findFirst({
    where: {
      telegramId: userContext.telegramId,
      isActive: true,
      department: {
        code: departmentCode,
        isEnabled: true,
      },
    },
  })

  if (isDeptAdmin) {
    return true  // ✅ معيّن → دخول مباشر
  }

  // 3. فحص SubFeatureAdmin
  const hasSubFeatureAccess = await Database.prisma.subFeatureAdmin.findFirst({
    where: {
      telegramId: userContext.telegramId,
      isActive: true,
      subFeature: {
        code: { startsWith: `${departmentCode.replace('-management', '')}:` },
        isEnabled: true,
      },
    },
  })

  if (hasSubFeatureAccess) {
    return true  // ✅ معيّن على وظيفة
  }

  // 4. فحص minRole (آخر شيء)
  const minRole = department.minRole || 'ADMIN'
  return this.hasMinRole(userContext, minRole as Role)
}
```

---

#### 1.2 إصلاح `canAccessSubFeature`

**نفس المنطق:**
1. SUPER_ADMIN
2. التعيينات (DepartmentAdmin أو SubFeatureAdmin)
3. minRole

---

#### 1.3 إصلاح `MenuBuilder.buildSubMenu`

**التعديل:**
```typescript
// بدلاً من فحص minRole مباشرة:
if (PermissionService.hasMinRole(userContext, minRole)) {
  accessibleSubFeatures.push(subFeature)
}

// يجب:
// 1. إضافة الزر إذا كان SUPER_ADMIN
// 2. إضافة الزر إذا كان DepartmentAdmin
// 3. إضافة الزر إذا كان SubFeatureAdmin
// 4. فحص minRole فقط للباقين
```

---

### المرحلة 2: إضافة الفحوصات الوقائية 🛡️

**الأولوية:** 🟡 متوسطة

#### 2.1 فحص عند تعيين DepartmentAdmin

**الملف:** `src/bot/features/hr-management/handlers/section-management.handler.ts`

**الإضافة:**
```typescript
// قبل سطر: await Database.prisma.departmentAdmin.create

// التحقق من minRole
const department = await Database.prisma.departmentConfig.findUnique({
  where: { code: 'hr-management' }
})

const user = await Database.prisma.user.findUnique({
  where: { id: userId }
})

const minRoleLevel = ROLE_HIERARCHY[department.minRole as Role] || 0
const userRoleLevel = ROLE_HIERARCHY[user.role] || 0

if (userRoleLevel < minRoleLevel) {
  await ctx.editMessageText(
    `❌ **لا يمكن تعيين هذا المستخدم**\n\n`
    + `دوره الحالي: **${user.role}**\n`
    + `الحد الأدنى المطلوب: **${department.minRole}**\n\n`
    + `يجب ترقية المستخدم أولاً.`,
    {
      parse_mode: 'Markdown',
      reply_markup: new InlineKeyboard()
        .text('⬅️ رجوع', 'hr:section:admin:menu')
    }
  )
  return
}

// المتابعة بالتعيين...
```

---

#### 2.2 فحص عند تعيين SubFeatureAdmin

**نفس المنطق السابق** لكن للوظائف الفرعية.

---

#### 2.3 فحص عند تحديد minRole للوظيفة

**عند إنشاء/تعديل واجهة تحديد minRole للوظائف:**

```typescript
const department = await Database.prisma.departmentConfig.findUnique({
  where: { code: departmentCode }
})

const deptLevel = ROLE_HIERARCHY[department.minRole as Role] || 0
const subLevel = ROLE_HIERARCHY[newMinRole as Role] || 0

if (subLevel < deptLevel) {
  return "❌ لا يمكن أن تكون minRole الوظيفة أقل من minRole القسم"
}
```

---

### المرحلة 3: إعادة بناء واجهة إدارة القسم 🎨

**الأولوية:** 🟢 منخفضة (بعد المرحلة 1 و 2)

#### الهيكل الجديد:

```
⚙️ إدارة قسم شئون العاملين
├── 🔐 إدارة الصلاحيات
│   ├── تحديد minRole للقسم
│   ├── تحديد minRole للوظائف الفرعية
│   └── عرض الصلاحيات الحالية
├── 👥 إدارة الأدمن
│   ├── تعيين DepartmentAdmin
│   ├── إزالة DepartmentAdmin
│   ├── تعيين SubFeatureAdmin
│   ├── إزالة SubFeatureAdmin
│   └── عرض جميع التعيينات
└── 🔄 التحكم في التشغيل
    ├── تفعيل/إيقاف القسم (isEnabled)
    └── تفعيل/إيقاف الوظائف الفرعية (isEnabled)
```

---

## 📊 جدول المقارنة: قبل وبعد

| الجانب | قبل الإصلاح | بعد الإصلاح |
|--------|-------------|-------------|
| **تعيين USER على قسم ADMIN** | ✅ يسمح (خطأ) | ❌ يمنع (صحيح) |
| **DepartmentAdmin يتجاوز minRole** | ❌ لا (خطأ) | ✅ نعم (صحيح) |
| **فحص minRole للوظائف** | ❌ غير موجود | ✅ موجود |
| **عرض الأزرار للمعيّنين** | ❌ تختفي (خطأ) | ✅ تظهر (صحيح) |
| **SUPER_ADMIN** | ✅ يعمل | ✅ يعمل |
| **GUEST** | ✅ ممنوع | ✅ ممنوع |

---

## ✅ معايير النجاح

### الاختبار 1: تعيين USER على قسم ADMIN
```
❌ يجب أن يُرفض
✅ رسالة خطأ واضحة
```

### الاختبار 2: DepartmentAdmin بدور أقل
```
✅ يدخل على القسم (بعد التعيين فقط من SUPER_ADMIN)
❌ لا يمكن تعيينه من البداية
```

### الاختبار 3: وظيفة بـ minRole أقل من القسم
```
❌ يجب منعها عند الإنشاء
✅ رسالة خطأ واضحة
```

### الاختبار 4: SUPER_ADMIN
```
✅ يرى كل شيء
✅ يتجاوز كل القيود
```

### الاختبار 5: GUEST
```
❌ لا يرى القائمة الرئيسية
✅ رسالة تطلب منه التسجيل
```

---

## 📝 ملاحظات إضافية

### حالات خاصة

#### حالة 1: ترقية مستخدم معيّن
```
السيناريو:
- USER معيّن كـ SubFeatureAdmin على وظيفة بـ minRole = ADMIN
- تم ترقيته إلى ADMIN

النتيجة:
- التعيين يبقى ساري
- الآن يمكن تعيينه على القسم كاملاً
```

#### حالة 2: تخفيض minRole للقسم
```
السيناريو:
- قسم minRole = ADMIN
- تم تخفيضه إلى MODERATOR

النتيجة:
- جميع التعيينات الحالية تبقى سارية
- يمكن الآن تعيين MODERATOR
```

#### حالة 3: رفع minRole للقسم
```
السيناريو:
- قسم minRole = MODERATOR
- يوجد DepartmentAdmin بدور MODERATOR
- تم رفع minRole إلى ADMIN

السؤال: ماذا نفعل بالـ DepartmentAdmin؟

الخيارات:
1. إزالته تلقائياً ❌ (قاسي)
2. الإبقاء عليه مع تحذير ⚠️ (مرن)
3. منع تغيير minRole ❌ (صارم جداً)

الاختيار المقترح: الخيار 2
```

---

## 🔗 الملفات ذات الصلة

### الملفات الأساسية
- `prisma/schema.prisma` - تعريف قاعدة البيانات
- `src/modules/permissions/permission-service.ts` - منطق الصلاحيات
- `src/modules/permissions/types.ts` - تعريف الأنواع
- `src/bot/features/registry/menu-builder.ts` - بناء القوائم
- `src/bot/features/main-menu.ts` - القائمة الرئيسية

### الملفات المتأثرة
- `src/bot/features/hr-management/handlers/section-management.handler.ts`
- `src/bot/features/hr-management/handlers/section-permissions.handler.ts` (جديد)

---

## 📅 الجدول الزمني

### الأسبوع 1: المرحلة 1 (الأولوية العالية)
- [ ] إصلاح `canAccessDepartment`
- [ ] إصلاح `canAccessSubFeature`
- [ ] إصلاح `MenuBuilder.buildSubMenu`
- [ ] اختبارات شاملة

### الأسبوع 2: المرحلة 2 (الفحوصات)
- [ ] فحص تعيين DepartmentAdmin
- [ ] فحص تعيين SubFeatureAdmin
- [ ] فحص minRole للوظائف
- [ ] اختبارات الفحوصات

### الأسبوع 3: المرحلة 3 (الواجهة)
- [ ] تصميم الواجهة الجديدة
- [ ] إدارة الصلاحيات
- [ ] إدارة الأدمن
- [ ] التحكم في التشغيل
- [ ] اختبارات نهائية

---

## 🎯 الخلاصة

نظام الصلاحيات الحالي **مبني بشكل جيد** لكنه يحتاج:

1. ✅ **إصلاح ترتيب الفحوصات** (التعيينات قبل minRole)
2. ✅ **إضافة فحوصات وقائية** (عند التعيين والإنشاء)
3. ✅ **واجهة إدارة محسّنة** (سهلة ومنطقية)

**بعد التنفيذ:**
- 🎯 النظام يتبع المتطلبات 100%
- 🔒 الأمان محكم
- 🎨 الواجهة سهلة الاستخدام
- 📚 التوثيق شامل

---

**تاريخ آخر تحديث:** 29 أكتوبر 2025  
**الحالة:** قيد التنفيذ  
**المسؤول:** فريق التطوير
