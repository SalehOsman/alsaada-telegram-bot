# 📋 معايير المشروع

**آخر تحديث:** 2025-01-26

---

## 🎯 المبادئ الأساسية

### 1. الاحترافية
- ✅ كود نظيف ومنظم
- ✅ توثيق واضح ومفيد
- ✅ معالجة الأخطاء شاملة
- ✅ اختبارات كافية

### 2. البساطة
- ✅ كتابة الحد الأدنى من الكود المطلوب
- ✅ تجنب التعقيد غير الضروري
- ✅ حلول مباشرة وواضحة
- ❌ لا verbose implementations

### 3. التنظيم
- ✅ كل ملف في مكانه الصحيح
- ✅ هيكل واضح ومنطقي
- ✅ تسمية موحدة ومفهومة

---

## 📁 تنظيم الملفات

### المسار الرئيسي (Root)
**يحتوي فقط على:**
- ✅ `README.md` - وصف المشروع
- ✅ ملفات الإعداد (`.env`, `package.json`, `tsconfig.json`, etc.)
- ✅ ملفات Git (`.gitignore`)

**ممنوع:**
- ❌ ملفات توثيق إضافية
- ❌ ملفات اختبار
- ❌ سكريبتات
- ❌ أي ملفات `.md` غير README

### التوثيق
**المكان:** `Documentation_Pro/`

**التنظيم:**
```
Documentation_Pro/
├── 00_فهرس_رئيسي/
├── 10_دليل_المطور/
├── 20_دليل_المستخدم/
├── 40_قواعد_البيانات/
├── 50_أدلة_عملية/
├── 90_وحدات_جاهزة/
└── ...
```

### السكريبتات
**المكان:** `scripts/`
- seed scripts
- migration scripts
- utility scripts

### الاختبارات
**المكان:** `tests/`
```
tests/
├── unit/
├── integration/
├── e2e/
└── performance/
```

### قاعدة البيانات
**المكان:** `prisma/`
```
prisma/
├── schema.prisma          # Schema فقط
├── seed.ts               # Seed رئيسي
├── seeds/                # Seed files
├── migrations/           # Migrations
└── 00_README_START_HERE.md  # دليل سريع فقط
```

---

## 🔤 التسمية

### الملفات
- **TypeScript/JavaScript:** `kebab-case.ts`
  - مثال: `leave-schedule.service.ts`
- **التوثيق:** `UPPER_CASE.md` أو `عربي.md`
  - مثال: `QUICK_START.md` أو `نظام_الإجازات.md`

### المجلدات
- **الكود:** `kebab-case/`
  - مثال: `hr-management/`
- **التوثيق:** `عربي/` أو `English/`
  - مثال: `90_وحدات_جاهزة/`

### Git Commits
```
<type>: <description>

Types:
- feat: ميزة جديدة
- fix: إصلاح خطأ
- refactor: إعادة هيكلة
- docs: توثيق
- test: اختبارات
- chore: مهام صيانة
```

**أمثلة:**
```
feat: إضافة نظام الإجازات - المرحلة 1
fix: إصلاح حساب أيام التأخير
refactor: تنظيف المشروع - نقل التوثيق
docs: تحديث دليل المطور
```

---

## 🔄 سير العمل

### عند إضافة ميزة جديدة:

1. **التخطيط**
   - مراجعة التوثيق
   - تحديد المتطلبات
   - تصميم الحل

2. **التنفيذ**
   - كتابة الكود
   - إضافة التعليقات بالعربية
   - معالجة الأخطاء

3. **الاختبار**
   - اختبار يدوي
   - كتابة اختبارات (إن لزم)

4. **التوثيق**
   - تحديث التوثيق
   - إضافة أمثلة

5. **Git**
   - Commit واضح
   - نسخة احتياطية (إن لزم)

---

## 📝 التعليقات

### في الكود
```typescript
// ✅ جيد: تعليق واضح بالعربية
// حساب موعد الإجازة القادمة بناءً على دورة العمل
const nextLeaveDate = calculateNextLeave(employee);

// ❌ سيء: تعليق غير مفيد
// calculate next leave
const nextLeaveDate = calculateNextLeave(employee);
```

### في التوثيق
- ✅ واضح ومباشر
- ✅ أمثلة عملية
- ✅ منظم بعناوين
- ❌ لا حشو أو تكرار

---

## 🎨 الكود

### المبادئ
- **DRY**: Don't Repeat Yourself
- **KISS**: Keep It Simple, Stupid
- **YAGNI**: You Aren't Gonna Need It
- **Single Responsibility**: كل دالة/ملف له مسؤولية واحدة

### الأسلوب
```typescript
// ✅ جيد: بسيط ومباشر
async function createLeave(data: LeaveData) {
  const leaveNumber = generateLeaveNumber();
  return await prisma.hR_EmployeeLeave.create({
    data: { ...data, leaveNumber }
  });
}

// ❌ سيء: معقد بدون داعي
async function createLeave(data: LeaveData) {
  try {
    const currentYear = new Date().getFullYear();
    const lastLeave = await getLastLeave();
    const nextNumber = lastLeave ? extractNumber(lastLeave) + 1 : 1;
    const leaveNumber = `LV-${currentYear}-${String(nextNumber).padStart(3, '0')}`;
    // ... 20 سطر إضافي
  } catch (error) {
    // ... معالجة معقدة
  }
}
```

---

## ✅ Checklist قبل Commit

- [ ] الكود يعمل بدون أخطاء
- [ ] التعليقات واضحة بالعربية
- [ ] الملفات في أماكنها الصحيحة
- [ ] التوثيق محدث
- [ ] Commit message واضح
- [ ] لا ملفات غير ضرورية

---

**هذه المعايير إلزامية لجميع التطويرات!**
